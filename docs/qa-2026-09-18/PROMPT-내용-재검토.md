# 학습 내용 재검토 — 9/18 감사 뒤 고친 글 전부 + 안 바뀐 강의 표본

## 소유자용 안내

**왜 하나요.** 최종 출시 관문은 "글이 화면에 빠짐없이 나오는지" 와 "6단계에서 찾은 오류 종류가 다시
생기지 않았는지" 는 보지만, **글 자체가 맞는지를 다시 읽지는 않습니다.** 9/18 감사 뒤로 고친 내용 파일이
1,149개인데, 고친 수정 세션이 스스로 확인한 것뿐입니다(점검 세션은 "고친 대로 운영에 올라갔는지" 와 일부
표본만 봤습니다). 소유자 결정 2026-09-24: **"그래 1번2번 넣자"**.

1. 9/18 뒤 바뀐 글을 전부 — 고치기 전과 후를 나란히 놓고 다시 읽습니다.
2. 안 바뀐 강의를 과정마다 5% 뽑아 한 줄씩 읽어, 9/18 감사가 놓친 것이 얼마나 되는지 가늠합니다.

**누가.** 새 세션 하나. 글을 고친 수정 세션도, 3차 점검 세션도 아닙니다. 그래도 같은 AI 계열이라 독립 검토는 아닙니다.

**언제.** 최종 출시 관문과 **같이** 돕니다. 브라우저를 쓰지 않아 관문을 방해하지 않습니다.
틀린 것이 나오면 수정 세션이 관문 0(전체 화면 검사)이 끝난 뒤 한 번에 고치고, 고친 강의만 다시 봅니다(관문 15).

**시작.** 새 세션을 열고 이 한 줄을 붙이세요:

> docs/qa-2026-09-18/PROMPT-내용-재검토.md 의 ==== 사이 명령대로 학습 내용 재검토를 해.

====================== 여기서부터 명령 ======================

학습 내용 재검토다. 읽고 판정만 해라. 고치지 마라.

■ 지킬 것
  - 내용 파일(content/ · src/lib 의 글 파일) · 코드 · 음성 클립을 바꾸지 마라. 옆 "3차 점검 수정" 세션이 최종 출시 관문을
    돌리는 중이고, 그 검사는 작업 트리의 내용 파일로 "화면에 나와야 할 글" 을 계산한다 — 한 글자만 바꿔도 가짜 누락이 난다.
  - 브라우저를 열지 마라(관문이 브라우저 둘을 쓰고, 이 컴퓨터 메모리가 빠듯하다). 화면에 나오는 글은
    docs/qa-2026-09-18/scripts/lib/expectations.cjs, 소리 내는 글은 scripts/lib/spoken-texts.cjs 로 안다.
  - 쓰는 곳은 docs/qa-2026-09-18/내용-재검토/ 폴더(이 작업의 스크립트 · 기록 · 판정)와 docs/qa-2026-09-18/내용-재검토-결과.md 뿐.
    git 은 그 파일만 이름으로 add · 커밋. git add -A · git add . · git stash 금지. 푸시 · 배포 금지.
  - CNN · GVA(폐지 과정)와 basics · middle(열리지 않는 과정)은 보지 마라.
  - 법률 문서 · 결제 · 저작권은 소유자 몫이다 — 판정 대상이 아니고 보고에 꺼내지도 마라.
  - 이용권 · 관리자 화면 · /api/ 를 부르지 마라. .env.local 값을 출력하지 마라.
  - 원본 교재(소유자 바탕화면 랩자료모음 — 읽기 전용)는 참고만 해라. 원본이 정답은 아니다. 맞는지는 실제로 맞는지로 판정한다.
  - 소유자가 이미 정한 것은 다시 따지지 마라: 소유자-결정표-6단계.md · 7단계-작업기록.md 의 결정표(7-4 f 22개 · 2026-09-24 아침 답)
    · bug-register.md 의 소유자 결정 · "낱말 소리는 화면에 보이는 뜻의 발음". 정한 것이 분명히 틀렸다고 보이면 틀림으로 세지 말고
    '결정과 다름' 칸에 따로 적어라.

■ 기준 — 시작 전에 고정한다. 기록 파일 맨 위에 그대로 적고 끝까지 바꾸지 마라
  - 기준 판 = 9d6e15d (9/18 감사가 본 운영 main — K-IG_Commercial_Release_Readiness_Report.md 3행).
    지금 판 = HEAD. 운영과 내용이 같아야 한다 — 시작할 때 git diff --name-only a6ff936 HEAD -- content src/lib 의 글 파일이 0 인지 확인해 적어라.
  - 틀림(고쳐야 함):
    ① 영어가 틀림 — 문법 · 낱말 · 철자 · 뜻을 바꾸는 문장부호
    ② 한국어 뜻이 영어와 다름 · 한국어 맞춤법이 틀림
    ③ 정답 · 대체 답이 문제와 안 맞음 — 맞는 답을 막거나 틀린 답을 받음
    ④ 사실이 틀림 — 날짜 · 이름 · 숫자
    ⑤ 소리 내는 글이 화면 글과 다름 · 발음이 화면에 보이는 뜻과 다름
    ⑥ 힌트 · 칩 · 제목 · 설명이 그 문장 · 강의와 안 맞음
    ⑦ 고치다 생긴 흔적 — 잘린 문장 · 겹친 낱말 · 남은 기호 · 두 문장이 붙음 · 한 강의 안에서 같은 글이 서로 다르게 고쳐짐
  - 틀림이 아님: 문체 취향 · 영국/미국 표기(한 강의 안에서 섞이지 않으면) · 원본과 다름 그 자체.
  - 판단 필요: 둘 다 말이 되는데 하나를 골라야 하는 것 · 교육 방침이 걸린 것 → 소유자 결정표로(추천안과 까닭).
  - 심각도(9/18 감사와 같은 넷): 심각 = 틀린 영어나 틀린 뜻을 가르침 · 높음 = 채점이 틀리거나 영어와 한국어가 서로 달라 학습자를 오도
    · 중간 = 헷갈리게 하지만 오도는 아님(오타 · 문장부호) · 낮음 = 띄어쓰기 · 한 강의 안 표기 흔들림.
  - 틀림마다 적을 것: 과정 · 강의 · 파일 · 칸(JSON 경로) · 고치기 전(9d6e15d) · 지금 · 왜 틀렸나 · 고칠 글 · 심각도 · 확신(높음/보통).
    보고 전에 그 강의의 화면 글(expectations)에서 학습자가 그 글을 실제로 보거나 듣는지 확인해라 — 안 닿는 칸이면 '화면에 안 닿음' 으로 따로.

■ 1 — 9/18 뒤 바뀐 글 전부
  - 목록: 기준 판과 지금 판의 내용 파일을 글 조각(JSON 문자열 값 하나) 단위로 대조해라. 범위는 content/ 전부(lessons 6과정 · courses ·
    ld_english_scripts.json · voca_dictionary.json)와 src/lib 의 글 파일(readingSentences.json · readingVocabulary.json ·
    curriculumPresentation.ts · vocaSpeech.ts 의 발음 표). 기준 판 파일은 git cat-file --batch 로 한 번에 읽어라. 바뀜 · 새로 생김 · 없어짐 모두.
    (2026-09-24 점검 세션이 센 파일 수: 1,149.)
  - 완전성 증명: 목록에 든 파일 수 = git diff --name-only 9d6e15d HEAD -- <같은 경로> 의 파일 수.
    일부러 깨기: 사본 내용 파일 하나의 글자 하나를 바꾼 판으로 돌리면 목록에 한 줄이 더 나와야 한다(exit 1).
  - 줄마다 '화면 · 소리에 닿음 / 안 닿음' 을 expectations · spoken-texts 로 가르고, 닿는 줄은 전부 판정해라.
    안 닿는 줄은 수만 세고, 그중 20줄을 뽑아 정말 안 닿는지 읽어 확인해라.
  - 판정은 강의 단위로 — 바뀐 줄만 보지 말고 그 강의의 앞뒤 글과 함께 읽어라(한국어 뜻 · 정답 · 힌트 · 소리가 서로 맞는지).

■ 2 — 안 바뀐 강의 표본
  - 과정마다 쪽(주소)의 5%, 최소 6쪽: STUDENT 82 → 6 · VOCA 195 → 10 · GRAMMAR I 194 → 10 · GRAMMAR II 88 → 6 ·
    LISTENING 276강(본 · 대본 552쪽) → 14강 28쪽 · READING 256강(512쪽) → 13강 26쪽. 합 86쪽.
    씨앗을 고정한 무작위로 뽑고, 씨앗과 뽑은 목록을 기록에 적어라.
  - 뽑은 쪽에서 학습자가 보고 듣는 글 전부를 읽어라(expectations). 찾은 틀림은 '9/18 뒤 바뀐 글에 있음'(1 과 겹침) /
    '안 바뀐 글에 있음'(9/18 감사가 놓친 것)으로 갈라라.
  - 숫자: 과정마다 쪽 N · 글 N · 놓친 틀림 N · 쪽당 놓친 비율과 95% 위쪽 한계(0 이면 3/N).
    놓친 틀림이 한 과정에서라도 나오면 "그 과정을 전부 다시 읽을지" 를 숫자와 함께 소유자 결정 칸에 올려라.

■ 진행 · 기록 — 세션이 압축돼도 이어지게
  - docs/qa-2026-09-18/내용-재검토/기록.md 맨 위에 위 기준을 그대로 적고, 그 아래에 배치마다 한 줄(시각 · 읽은 줄 · 틀림 · 판단 필요).
  - 판정은 docs/qa-2026-09-18/내용-재검토/판정.json 에 줄마다 쌓아라(목록 id · 판정 · 심각도 · 까닭 · 고칠 글).
    압축되면 기록.md 와 판정.json 부터 다시 읽고 이어서 해라.
  - 첫 1시간이 지나면 실제 속도(시간당 판정한 줄)로 남은 시간을 기록.md 에 적어라. 어림으로 적지 마라.

■ 보고 — docs/qa-2026-09-18/내용-재검토-결과.md
  - 맨 위에 소유자용 한 줄 판정: 🟢 틀림 0 · 🟡 낮음 · 중간만 · 🔴 심각 · 높음 있음. 소유자는 기술 용어를 읽지 못한다 — 쉬운 말로.
  - 숫자: 1 — 바뀐 조각 N(닿음 N · 안 닿음 N) · 판정 N · 틀림 N(심각 · 높음 · 중간 · 낮음) · 판단 필요 N · 결정과 다름 N.
    2 — 과정마다 위 표본 숫자.
  - 틀림 표(심각도 순) — 수정 세션이 그대로 고칠 수 있게(파일 · 칸 · 지금 · 고칠 글). 영어나 소리 내는 글을 바꾸는 줄에는
    '새 음성 클립 필요' 를 표시해라(클립 이름이 글에서 나온다 — 글이 바뀌면 옛 클립은 안 불린다).
  - 판단 필요 표 — 번호 · 추천안 · 까닭. 소유자가 번호로 답할 수 있게.
  - 마지막에 이 문장을 그대로 적어라:
    "이 재검토는 글을 고친 세션과 다른 세션이 했지만 같은 AI 계열입니다. 독립적인 검토가 아닙니다."

====================== 여기까지 명령 ======================

## 결과를 받으면 — 최종 출시 관문 15

- 수정 세션: 관문 0(전체 화면 검사)이 끝난 뒤 틀림 표를 고친다. 영어 · 소리 내는 글을 바꾸면 같은 작업 안에서
  `node scripts/generate-azure-ava.mjs` 로 새 클립 → R2 먼저 → 같은 클립 이름 덮어쓰기 금지.
- 판단 필요는 소유자 결정 뒤에 고친다.
- 배포는 소유자 허락으로. 배포 뒤 기대 글이 달라진 강의만 3화면 다시 돌려 관문 0 기록에 더하고, 그다음 관문 1~14.
- 3차 점검 세션이 고친 글 N/N 을 운영에서 다시 잰다(PROMPT-재점검.md 방식).
