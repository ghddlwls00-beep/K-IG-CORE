# 최종 출시 관문 — 돈을 받기 전에 마지막으로 통과해야 하는 것

모든 수정과 재점검이 끝난 뒤, **마지막에 한 번만** 돌리는 명령서입니다.
`====` 로 둘러싼 부분만 복사해서 수정 세션에 붙이세요.

## 소유자용 안내

2026-09-18 감사 보고서의 판정은 **NO-GO** 였습니다. GO 로 바뀌려면 아래 10가지가
모두 충족돼야 합니다. 그중 **4개는 감사 시점에 이미 충족**돼 있었고, **3개는 사장님이
직접 처리하기로 한 항목**이며, 나머지가 수정 대상이었습니다.

| # | 조건 | 감사 시점 | 누가 |
|---|---|---|---|
| 1 | 법정 표시 8종 게시 | 미충족 | **소유자** |
| 2 | 구매 주소(`NEXT_PUBLIC_PURCHASE_URL`) 설정 | 미충족 | **소유자** |
| 3 | READING 현대 저작물 발췌 18건 검토 | 미충족 | **소유자** |
| 4 | ~~음성 클립 404~~ | **철회** (감사 도구 오류) | — |
| 5 | 심각한 사실 오류 3건 수정 | 미충족 → 4단계 | 수정 세션 |
| 6 | 이용권 코드 공백 우회 차단 | **해결** (1단계) | — |
| 7 | 커버리지 NOT TESTED 0 | 충족 | — |
| 8 | 정답인데 오답 처리 0건 | 충족 | — |
| 9 | 이용권 없이 열리는 유료 주소 0건 | 충족 | — |
| 10 | 공개 자바스크립트에 유료 본문 유출 0건 | 충족 | — |

**이 관문의 목적은 "아직 충족" 이 여전히 참인지 다시 확인하는 것입니다.** 7~10번은 감사
시점에 참이었지만, 그 뒤로 수백 개 파일을 고쳤습니다. 고치는 과정에서 깨졌을 수 있습니다.

> **2026-09-23 고침 — 옛 점검 기록으로는 이 관문을 통과할 수 없습니다.**
> 9/18 감사의 "화면에 글자 있음" 판정 82,315건 중 69,569건은 검사 도구가 화면이 비어도 통과시키던
> 것으로 드러나 무효가 됐습니다(findings-log `CONTENT-VOID-0918`). 그래서 이 관문은 **7단계에서 고친
> 도구로 6개 과정 전체를 새로 돌린 기록**으로만 판정합니다(관문 0). 같은 날 관문 9~14 를 더했고,
> 관문 3 을 공개 JSON 까지 넓혔습니다 — 4·5단계에서 유료 낱말 목록이 공개 검색 파일(JSON)로
> 나갈 뻔했는데, 옛 관문 3 은 자바스크립트만 봤습니다. 관문 4 의 문장 수도 바로잡았습니다.

> **2026-09-24 더함 — 관문 15 학습 내용 재검토 (소유자 결정 "그래 1번2번 넣자").** 이 관문은 글이 화면에 나오는지는
> 보지만 글이 맞는지는 다시 읽지 않았습니다. 새 세션이 [`PROMPT-내용-재검토.md`](PROMPT-내용-재검토.md) 로 9/18 뒤 바뀐
> 글 전부와 안 바뀐 강의 표본을 같은 때에 읽고, 틀린 것은 관문 0 뒤 한 번에 고쳐 고친 강의만 다시 봅니다.

====================== 여기서부터 복사 ======================

최종 출시 관문이다. 고치지 마라. 확인만 해라. 문제를 찾으면 보고하고 멈춰라.
(예외 하나 — 관문 15: 따로 도는 학습 내용 재검토가 찾은 틀림은 관문 0 이 끝난 뒤 한 번 고친다. 순서는 관문 15 에.)

운영(https://k-ig-core.vercel.app)에서 확인한다. 로컬 결과는 근거가 되지 않는다.

■ 지킬 것
  이미 등록된 감사용 프로필을 써라. 새 기기를 등록하지 마라.
  이용권 코드·비밀번호·관리자 PIN 을 타이핑하지 마라.
  /api/license/ 중 상태를 바꾸는 것과 /api/admin/ 의 변경 계열을 부르지 마라.
  CNN · GVA 는 폐지 과정이다. 건드리지 마라.
  법률 문서·결제 연결·저작권 검토는 소유자가 직접 한다. 확인 대상이 아니다.

■ 관문 0 — 먼저 새 검사로 6개 과정 전체를 다시 돌려라 (아래 관문들의 근거)
  9/18 기록의 "화면에 글자 있음" 판정은 무효다(findings-log CONTENT-VOID-0918). 옛 기록을 쓰지 마라.
  7단계에서 고친 도구(칸별 읽개 lib/containers.cjs, 서버 오류 기록 보존, STUDENT 받아쓰기 조립)로
  운영에서 6개 과정 전 페이지를 3개 화면 크기로 다시 돌려라. 표본이 아니다.
  7단계 7-1 m 으로 더 보게 된 것: LISTENING 본 쪽 받아쓰기(타일 · 타이핑 둘 다 — 9/17 부터 브라우저로 한 번도 안 봤던 곳),
  휴대폰 크기의 받아쓰기 조립, STUDENT 3단계 끝 '이 강의 학습 완료'(누름 → 새로고침 → 되돌림). 이것들이 기록에 없으면 BLOCKED 다.
  시작 전에 도구가 실패를 잡는지 한 번 더 증명해라 — proof-summary.cjs 의 12개 시험이
  지금 코드에서도 "실패 잡음 확인" 인지. 아래 관문 5·6·9·10·11 은 이 새 기록으로만 판정한다.
  기록 파일마다 만든 날짜가 마지막 배포 뒤인지 확인하고, 그 목록을 보고에 붙여라.
  (관문 15 로 고친 배포가 끼면: 그 배포로 기대 글이 달라진 강의는 그 배포 뒤 기록만 쓰고, 나머지 강의의 기록은 그대로 둔다.
   코드가 바뀌었으면 그 파일을 쓰는 과정 전부를 다시 돌린다 — import 를 따라 과정 목록을 만들어라(예: LdLearningView → LISTENING 전부 ·
   vocaUtils → PhonicsLearningView → VOCA 전부 · listeningUtils → LISTENING · STUDENT). 여러 과정이 함께 쓰는 파일(layout · speech ·
   ProgressProvider · [course]/[lesson]/page.tsx 등)이 바뀌면 전부 다시. 다만 공용 파일이라도 ① git diff 가 그 몇 줄뿐이고 ② tsc · 빌드 0 이며
   ③ 그 파일이 내놓는 값(예: curriculumPresentation 의 제목 · 부제목, vocaWordSpeech · 소리 글의 SSML · 클립 이름)을 모든 강의 · 과정 목록에
   고치기 전 · 뒤 판으로 계산해 달라진 쪽 목록을 도구로 내고, 깨기(다른 줄 하나를 바꾼 사본 → 목록 밖이 달라짐 · exit 1)를 통과하면
   그 목록의 쪽 × 3화면만 다시 돈다 — 하나라도 못 보이면 전부(2026-09-24 점검 세션, 판단 3 · hv-50 T34). 어느 강의의 기대 글이 달라졌는지는 expectations 를 배포 전 · 뒤 판으로
   대조해 목록으로. 고친 배포 뒤에 다시 돌 것이 확실한 과정은 첫 스윕에서 표본만 돌려도 된다 — 같은 과정을 두 번 다 도는 시간을 아끼려고
   (2026-09-24 점검 세션: 재검토가 LISTENING 힌트 칩 · VOCA 퀴즈 오답 보기를 코드에서 찾음).)
  관문 0 이 도는 동안 내용 파일(content/ · src/lib 의 글 파일)을 바꾸지 마라 — 기대 글이 틀어져 가짜 누락이 난다.

■ 관문 1 — 이용권 없이 열리는 유료 주소가 0건인가
  로그아웃 상태로 유료 주소 1,595개 전부를 확인해라. 표본이 아니다.
  숫자로: 잠긴 것 N / 열린 것 N.  열린 것이 1개라도 있으면 출시 불가다.

■ 관문 2 — 무료 체험 28개가 여전히 열리는가
  숫자로: 열린 것 N / 28.  28 미만이면 출시 불가다.
  소리도 — 먼저 audio-inventory.cjs 로 목록을 지금 내용으로 새로 만들고(옛 목록이면 가짜 경보 —
  2026-09-23 6단계 배포 때 실제로 8건), 로그아웃 상태로 docs/qa-2026-09-18/scripts/audio-check.cjs --anon: 무료 강의 클립 N/N 이
  200/206 audio, 유료 클립은 전부 403. node scripts/buildFreeSpeechKeys.mjs --check 는 exit 0 이어야 한다
  (2026-09-23 6단계 중간에 이미 낡았음 — 무료 강의 문장이 바뀌어서. 낡은 채 나가면 체험하러 온 사람만 소리가 막힌다).
  돈 낸 사람이 아니라 체험하러 온 사람이 막히는 것이라 매출이 그대로 사라진다.

■ 관문 3 — 공개 파일에 유료 본문이 새는가 (자바스크립트와 JSON 모두)
  로그인 없이 받을 수 있는 배포 파일 전체 — 자바스크립트 번들, /search-index.json 을 비롯한
  공개 JSON — 를 받아 유료 강의 본문 문자열이 들어 있는지 검사해라.
  규칙은 docs/qa-2026-09-18/scripts/probe-bundle-leak-all.cjs 와 같다 (VOCA 낱말표 줄 포함).
  숫자로: 검사한 파일 N개 / 유출 N건.  0이 아니면 출시 불가다.

■ 관문 4 — 정답인데 오답 처리되는 채점이 0건인가
  GRAMMAR 종합 평가 282강, LISTENING 받아쓰기 2,217문장, STUDENT 받아쓰기 402문장을 전부 돌려라.
  숫자로: 검사 N건 / 정답인데 오답 처리된 것 N건.  0이 아니면 출시 불가다.
  GRAMMAR 종합 평가는 drive-generic 이 판정하지 못한다(칸마다 쳐 봐도 [전체 시험 채점하기] 전에는 결과가 없어 FAIL 로 적힘 —
  2026-09-24 재점검 표본에서 499건 모두 이 종류). 그래서 docs/qa-2026-09-18/scripts/check-grammar-exam.cjs 로 운영에서 282강 전부:
  모범 답안 · 대체 답안을 넣고 채점 → 모두 ✓ 정답(100점)이어야 한다. 그리고 소유자 결정(2026-09-24)이 실제로 되는지 과정마다 몇 강:
  뜻이 반대인 답(not · n't 를 넣거나 뺌) → ✕ 오답(0점) · 작은 낱말 하나 더(the · a) → △ 부분 정답(70점)
  (docs/qa-2026-09-18/scripts/check-grammar-exam-variants.cjs --course grammar1|grammar2 --ids … — 2026-09-24 운영 gh1-006 4/4 기대대로.
   고치기 전 채점기라면 'I am not Korean.' 이 △ 70점이라 이 검사가 실패한다 — 채점 함수 단위 비교는 7단계 7-4 f ② 기록).

■ 관문 5 — 음성이 끊기는 곳이 없는가
  모든 과정의 음성 버튼을 눌러 재생 실패 수를 세라.
  숫자로: 누른 횟수 N / 재생 실패 N.  실패가 있으면 어느 강의인지 전부 적어라.

■ 관문 6 — 커버리지에 NOT TESTED 가 0인가
  관문 0 의 새 기록으로 build-coverage.cjs 를 다시 집계해라. 옛 기록을 섞지 마라.
  7단계 7-1 m 에서 고친 판으로: --since <관문 0 을 시작한 시각>(또는 관문 0 기록 파일을 --files 로) — 둘 다 없으면 도구가 멈춘다.
  강의×화면마다 가장 늦은 기록으로 세고, 도구 잘못 목록에 없는 BLOCKED 는 강의 BLOCKED 가 된다(고치기 전 판은 이것을 PASS 로 셌다).
  NOT TESTED 가 0이 아니면 출시 불가다.
  BLOCKED 가 있으면 각각 왜 막혔는지 사유를 적어라. 사유 없는 BLOCKED 는 미점검이다.
  NA(해당 없음)는 대신 본 검사(coveredBy)가 같은 기록에 PASS 로 있을 때만 덮인 것으로 센다.

■ 관문 7 — 심각한 사실 오류 3건이 실제로 고쳐졌는가
  content-fix-list.md 의 [Critical] 3건을 운영 화면에서 하나씩 열어 확인해라.
  고쳐진 내용이 화면에 나오는지, 그 문장의 음성이 새 내용으로 들리는지.

■ 관문 8 — 구매 주소가 설정됐는가
  배포된 페이지에서 "구매 링크 준비 중" 문구가 나오는지 확인해라.
  ※ 설정은 소유자가 한다. 너는 상태만 보고해라. 결제 화면에 들어가지 마라.

■ 관문 9 — 서버 오류(4xx·5xx)가 0인가
  9/18 에는 도구가 기록을 지워서 "판정 불가" 였다. 7단계에서 고친 도구의 관문 0 기록으로 세라.
  숫자로: 요청 기록이 있는 페이지 N / 전체 N · 4xx N · 5xx N. 의도된 것(잠긴 유료 음성의 403 등)은
  따로 세고 이유를 적어라. 요청 기록이 없는 페이지가 남으면 그 수를 적어라 — 0 이어야 한다.

■ 관문 10 — 화면에 있어야 할 글자가 전부 있는가 (새 검사)
  관문 0 기록에서 6개 과정 전부의 content 결과. 숫자로: 기대 N / 있음 N / 없음 N.
  없는 것은 강의·화면 크기·글자까지 전부 적어라. 알려진 것(보류 결정 대기)과 새것을 나눠라.

■ 관문 10-2 — STUDENT 문장이 원본과 같은가 (2026-09-23 원본 복원 뒤 새로)
  9/7 커밋 08b77af 가 STUDENT 14과를 원본에 없는 글로 바꾼 채 두 주가 지나 소유자가 휴대폰에서 찾았다.
  소유자 보관본(바탕화면 랩자료모음\최종 Lab\최신Lab\본사 Lab v1.01\Student\*.swf)을 scripts/swf-text.mjs
  로 꺼내 강의마다 문장을 대조해라: 원본에 없는 문장은 기록된 고침(4·5·6·7단계, 번호와 함께)만이어야 한다.
  숫자로: 강의 N · 문장 N · 원본에 있음 N · 기록된 고침 N · 설명 안 되는 것 0.

■ 관문 11 — BLOCKED 가 0인가
  9/18 의 STUDENT 받아쓰기 241강은 7단계에서 드라이버를 고쳤다(2026-09-24 재검사 82/82 PASS). 남은 BLOCKED 가 있으면 강의마다 사유.
  사유 없는 BLOCKED 는 미점검이고 출시 불가다. 관문 6 과 같은 고친 build-coverage 숫자로 세라(기록을 손으로 골라 세지 마라).

■ 관문 12 — 속도 (BUG-023)
  이용권 학습자 휴대폰 4G 최대 콘텐츠(LCP). measure-perf.cjs 로 **단독 실행**해라.
  숫자로: 초. 2.5초 이하면 통과. 넘으면 7단계 기록의 소유자 판단(수용 여부)을 인용해라.

■ 관문 13 — 6·7단계가 끝났는가
  6단계-진행.json 의 "미처리" 0 (이월 6E- 항목 포함), 7단계 작업기록의 항목 전부 결론.
  각 단계의 "소유자 결정 대기" 가 전부 결정됐는지. 결정 안 된 것은 번호와 함께 적어라.
  결정된 것은 반영됐는지도 — 6단계-진행.json 의 "보류" 수(7단계 7-4 f 뒤 0 이어야 함, 남으면 번호와 사유).
  6단계-작업기록.md 의 "최종 관문 검사 목록" 명령(예: check-voca-twins · check-voca-answer-leak ·
  check-voca-distractors · ld-chip-app-vs-lib · ld-chips --dupes)을 전부 다시 돌려 기대값과 같은지,
  일부러 깨기도 다시 — 6단계 뒤에 바뀐 것(7단계 포함)이 그 검사를 깨지 않았는지 숫자로.
  깨기가 안 깨지면(exit 0 · 0) 그 검사는 증명되지 않은 것이다 — 통과로 세지 말고 불통과로 적어라. '고치기 전 판' 을 git HEAD 로
  읽던 깨기는 고친 것이 커밋되면 안 깨진다(2026-09-24 점검 세션이 일곱 개 찾음 — PROMPT-7단계.md 7-1 n 에서 고정 판으로 고침).

■ 관문 14 — 감사 보고서를 새 숫자로 다시 냈는가
  K-IG_Commercial_Release_Readiness_Report.md 를 이 관문의 숫자로 새로 쓴 판을 만들고,
  verify-report.cjs 로 숫자가 증거와 맞는지 확인해라. 9/18 판의 숫자를 그대로 두지 마라.
  findings-log · bug-register 의 상태 칸이 실제와 같은지도 (7단계 기록 정리 뒤).

■ 관문 15 — 학습 내용 재검토 (소유자 결정 2026-09-24 "그래 1번2번 넣자")
  새 세션이 docs/qa-2026-09-18/PROMPT-내용-재검토.md 로 따로 돈다(브라우저 없음 · 내용 파일 안 바꿈): 9/18 감사(9d6e15d) 뒤 바뀐 글 전부 +
  안 바뀐 강의 표본 86쪽. 결과는 가지 claude/learning-content-review-8d209a 의 docs/qa-2026-09-18/내용-재검토-결과.md.
  표본에서 9/18 이 놓친 틀림이 나와(LISTENING 28쪽 중 21쪽) 소유자가 더 정함(2026-09-24 17:1x "1번2번다 할거야"): 표본 밖 안 바뀐 글
  전부(약 2만 줄 · 6과정)도 또 다른 새 세션이 전수 읽기 — 명령서는 같은 가지의 docs/qa-2026-09-18/내용-재검토/전수/PROMPT-전수-읽기.md,
  결과는 그 세션 가지의 docs/qa-2026-09-18/내용-재검토/전수/결과.md(가지 이름은 3차 점검 세션이 알린다).
  두 결과 모두 작업 트리가 아니라 가지에 있다 — git show <가지>:<경로> 로 읽고, 관문 뒤 푸시 전에 두 가지의 문서를 main 에 합쳐라.
  순서: 관문 0 스윕이 끝나면 두 결과(재검토 · 전수 읽기)를 받는다(아직이면 기다리고 기다린 시간을 적어라) → 두 틀림 표를 한 번에 고친다(영어 · 소리 내는 글을
  바꾸면 같은 작업 안에서 generate-azure-ava → R2 먼저 → 같은 클립 이름 덮어쓰기 금지) · 판단 필요는 소유자 결정 뒤에 →
  소유자 허락으로 배포 → 기대 글이 달라진 강의 전부 × 3화면을 다시 돌려 관문 0 기록에 더한다 → 그다음 관문 1~14.
  틀림이 0 이면 배포 없이 관문 1~14.
  숫자로: 재검토 판정 N · 틀림 N(심각 · 높음 · 중간 · 낮음) · 고침 N · 소유자가 '두기' 로 정함 N · 남음 N(0 이어야 통과) ·
  운영에서 고친 글 N/N(PROMPT-재점검.md 방식) · 다시 돈 강의 N · 표본의 놓친 틀림(과정마다 N · 95% 위쪽 한계).
  재검토나 전수 읽기가 끝나지 않았으면 불통과로 적어라. 숫자는 재검토 · 전수 읽기를 따로 적어라.
  '맞는 답인데 0점' 틀림은 재검토 표의 심각도(9/18 매김으로 중간)와 상관없이 출시 조건 8(정답인데 오답 처리 0건)에 걸린다 — 남으면 관문 4 도 불통과.
  소유자 결정(2026-09-25 03:1x, 수정 세션 창): 결정 A(문법 모든 문항을 흔한 맞는 영작으로 잼 — 전수 가지 채점/결과.md) 의 다른 정답 3,233 은 넣는다(C) ·
  넣어도 남는 막힘은 두기(D) — 출시 조건 8 은 '재검토 · 전수 · A 표에 오른 막힘 0' 으로 좁혀 판정한다. 보고의 한계 칸에 그대로 적어라:
  "가린 다시 재기에서 한 번 훑기가 찾은 비율은 약 60%(229 중 92 놓침) — 표에 없는 맞는 영작이 시험에서 점수를 잃는 경우가 남는다(소유자가 받아들임)."
  채점 흠 ②(반대말 접두어에 70점)는 조건 8 과 따로 '뜻이 반대면 0점'(2026-09-24 결정)에 걸리므로 고쳐져야 통과다.

■ 소유자 확인 두 가지 — 네가 할 수 없는 것이니 됐는지만 물어서 적어라
  - 이용권 실제 동작 시험지 (7단계가 만든 것) — 소유자가 했는가, 결과는
  - 실제 아이폰·안드로이드 30분 — 소유자가 했는가

■ 보고 형식
  관문마다 한 줄로: "관문 N — 통과 / 불통과, 근거 숫자".
  불통과가 하나라도 있으면 제일 위에 쓰고, 나머지도 끝까지 다 확인해라.
  하나 막혔다고 중간에 그만두지 마라. 사장이 전체 그림을 봐야 한다.

  마지막에 이 문장을 그대로 적어라:
  "이 점검은 수정을 한 세션이 직접 수행했습니다. 독립적인 검토가 아닙니다."

====================== 여기까지 복사 ======================

## 결과를 받으면

관문 0~15 가 전부 통과해도 **자동으로 GO 가 되지는 않습니다.** 소유자가 처리하기로 한
세 가지(법정 표시·구매 주소·저작권 검토)가 남아 있고, 그것까지 끝나야 GO 입니다.

그리고 아직 아무도 확인하지 않은 것이 하나 있습니다 — **실제 아이폰·안드로이드 기기**입니다.
자동 점검은 전부 컴퓨터의 브라우저로 했습니다. 출시 전에 실제 휴대폰으로 30분만
직접 만져 보시는 것을 권합니다. 특히 음성 재생과 받아쓰기 입력입니다.
