# 공통 기능 · 접근성 다시 (2026-09-26 새벽 — 최종 관문 뒤)

> 사장님 결정: "너 추천대로 하자 그럼" · "계속해"(2026-09-26 04:2x). 명령서 대조표(새 세션)에 앞서, 9/18 원래 명령서 §4(공통 기능) · §13(접근성 · 안정성) 가운데
> 최종 관문 16관문에 없던 것을 **지금 운영 사이트**로 다시 봄. 고친 것 없음(사이트 · 학습 내용 그대로) — 점검만.
> **이 점검은 최종 관문을 돌린 세션이 직접 했습니다. 독립적인 검토가 아닙니다.**

## 한 줄로

**77곳 모두 통과 · 일부러 깬 것은 모두 잡음.** 작은 제품 흠 둘을 새로 찾음(**BUG-034** 문장 사이 0.3초 동안 정지 · 일시정지가 안 먹음, **BUG-035** 잠금 표시 글자 대비 4.47:1) — 둘 다 출시를 막을 것은 아님(P3).
점검 도구 흠 다섯을 돌리는 중에 찾아 고쳤고, 그중 하나(태블릿 넘침 사각)는 최종 관문 기록에도 걸렸을 수 있어 4,869곳을 다시 셈 → **넘침 0**(관문 판정 그대로).

## 이용권 없이 한 까닭

04:26 ~ 04:31 사이 이 컴퓨터의 임시 폴더(TEMP)가 통째로 지워짐 — 이용권 원본 프로필(`kig-audit-licensed-profile`)과 사본 · 이 세션의 작업 메모까지.
이 세션은 지우는 명령을 돌리지 않았고 휴지통에도 없음. 그래서 **이용권 없는 새 브라우저**로 돌리고, 강의 쪽은 **무료 체험 강의**(과정마다 둘 — `src/lib/license.ts`
`FREE_PREVIEW_LESSON_IDS`)만 씀. 유료 강의와 같은 화면 코드. 목록 · 검색 · 이동 · 404 · 화면 크기는 원래 공개 쪽.

## 결과 (이용권 없음 · 운영 `https://k-ig-core.vercel.app` · 앱 dd12758)

| 부분 | 무엇을 | 결과 | 일부러 깨기 |
|---|---|---|---|
| A 과정 목록 | 6과정 강의 링크가 목록과 같은가 · 진도 숫자(본 강의 3 + 대본 쪽 5 완료 · 북마크 2를 넣고 '학습 진도율 3 / N개 완료' · 전체 · 북마크 · 미완료) · 북마크 · 미완료 거르기 | **24/24 PASS**(`common-0926-r2.jsonl`) | 없는 강의를 기대 · 완료 4 를 기대 → **12 FAIL**(`break-A`) |
| B 검색 | 번호 · 제목 · 한국어 · 없는 말 11가지 — 결과 수가 앱 검색 함수와 같고, 눌러서 그 강의로 가는가 | **11/11 PASS**(`common-0926.jsonl`) — 영어 낱말 'hospital' 은 결과 없음(사장님 결정 BUG-011 대로) | 도착 주소를 틀리게 기대 → **7 FAIL** |
| C 이동 | 첫 쪽 → 목록 → 강의 → 뒤로 → 앞으로 → 새로고침 · 강의 주소 바로 열기 + 새로고침 · 없는 주소 6개 404 | **8/8 PASS** | 뒤로를 /nope 로 · pr001 을 404 로 기대 → **2 FAIL** |
| D 빠르게 · 여러 번 | 6과정 한 쪽씩 재생 ↔ 정지 연타 6 + 두 번 누르기 · 단계 빨리 바꾸기 3바퀴 → 멈춘 뒤 한 번 더 재생이 소리 냄 · '다음 강의' 를 불러오는 중에 연달아 → 주소 = 화면 강의 · 받아쓰기 '정답 채점하기' 6번 연타 → 완료 수 한 번만 | **9/9 PASS**(`common-0926-r3.jsonl`) | 누를 때마다 오류를 심음 → **9/9 FAIL** |
| E 오래 켜 두기 | 목록 ↔ 강의 60번(모두 화면 안 이동) · 같은 목록 쪽에서 GC 뒤 두 번 잼 | **PASS** — 메모리 4.8 → 5.2MB · DOM 275 → 275 · 이벤트 358 → 358(문턱 15MB · 50%) | 0.25초마다 1MB 새는 코드를 심음 → **FAIL**(76 → 407MB) |
| F 화면 크기 | 첫 쪽 · 과정 목록 6 · /t/voca × 컴퓨터 · 태블릿 · 휴대폰 — 가로 넘침(화면 폭 기준) · 화면 밖 단추 · 잘린 글 · 그림 | **24/24 PASS**(`common-0926-r4.jsonl`) | main 을 1,600px 로 → **24/24 FAIL**(`break-F-r4`) |
| 접근성 | 15쪽 × 밝은 · 어두운 테마: 글자 대비 · 24px 안 되는 단추 · 이름 없는 단추 · alt 없는 그림 · Tab 40번 초점 표시 | 9/18 과 같음 — **하나만 다름**: STUDENT 목록 밝은 테마 '🔒 잠금' 19개 4.47:1(기준 4.5) = BUG-035(이용권 없는 사람에게만 보임) | 글자를 흐리게 · 초점 표시를 지움 → 대비 낮음 0 → 105 · 초점 없음 0 → 78 |
| 관문 기록 다시 세기 | 최종 관문 스윕 기록 4,869곳(강의 × 화면)의 scrollWidth 를 화면 폭(1,366 · 768 · 390)에 댐 | **넘침 0**(가장 넓은 값 1,351 · 768 · 390) | 기록 한 줄을 1,601px 로 → **넘침 1 · exit 1** |

막은 STUDENT 진도 서버 쓰기: 0(이용권이 없어 보낼 것이 없었음).

## 새로 찾은 제품 흠 (둘 다 P3 — 출시를 막지 않음)

- **BUG-034 — 위 플레이어: 문장과 문장 사이 약 0.3초 동안 '정지' · '일시정지' 가 안 먹음** (READING pr001 에서 확인 · 세 번씩 되풀이)
  - 문장 가운데서 누르면: 정지 · 일시정지 모두 제대로(3/3).
  - 문장 사이에서 누르면: '정지' 단추가 꺼져 있어 눌러도 그대로 다음 문장이 나옴(3/3) · '재생/일시정지' 는 **첫 문장부터 다시 시작**(3/3).
  - 까닭(코드): 문장이 끝날 때마다 `src/lib/speech.ts:692`(finishRun) 가 '말하는 중 아님' 을 알리고, 다음 문장은 `:1175` 의 0.3초 틈 뒤에 시작 →
    `src/components/AudioPlayer.tsx:201` 정지 단추 켜짐 = 말하는 중 · `:103-107` 말하는 중이 아니면 `startQueue(0)`(처음부터).
  - 문장 길이 2 ~ 3초에 틈 0.3초 — 누름 열 번에 한 번꼴. 같은 위 플레이어를 쓰는 다른 과정도 코드상 같음(이번엔 READING 만 확인).
  - 증거: `results/bug034-stop-in-gap.txt` · `bug034-toggle-in-gap.txt` · `bug034-timeline.txt`(시각별 소리 사건).
- **BUG-035 — STUDENT 과정 목록(밝은 테마) '🔒 잠금' 표시 글자 대비 4.47:1** — 기준(WCAG AA 4.5:1)에 조금 못 미침. 잠긴 장에 붙는 표시(`CourseDashboard.tsx:466`)라
  앞 장을 아직 안 끝낸 학습자 · 이용권 없는 사람에게 보임(9/18 은 이 표시가 안 보이는 상태로 재어 안 나옴). 19개.

## 돌리는 중에 찾아 고친 점검 도구 흠 (제품 흠 아님)

| # | 흠 | 어떻게 드러났나 | 고침 |
|---|---|---|---|
| 1 | 장 펼치기가 글로 걸러 '무료' 가 든 장 머리를 건너뜀 — 이용권 없이 보면 첫 장 머리에 '1·2강 무료 체험' | 첫 판 STUDENT 목록 빠짐 6 = s1-1 ~ s1-6 | 접힌 장(aria-expanded="false")을 글 없이 펼침 → 82/82 |
| 2 | 재생 단추 글이 🔊 → ⏹️(재생 → 일시정지)로 바뀌어 다시 못 찾음 — 연타 6번 중 1 ~ 2번만 누름 | 첫 판 '재생 연타 1/6' | 처음 찾은 단추에 표를 달아 같은 단추를 6번 → 6/6 |
| 3 | 오래 켜 두기의 '화면 안 이동' 표를 한 번만 달아, 한 번 새로 뜨면 뒤를 모두 '새로 뜸' 으로 셈 | 첫 판 화면 안 이동 0 · BLOCKED | 이동마다 표를 새로 → 60/60 |
| 4 | '멈춘 뒤 한 번 더 재생' 을 도구의 멈춤(__kigStop — 화면의 &lt;audio&gt; 만)으로 하다가, 앱 정지 단추를 문장 사이에 누름 | 둘째 판 pr001 '새 소리 false' — 누른 단추를 적어 보니 꺼진 정지 단추 → BUG-034 를 찾음 | 앱 '정지' 가 켜질 때까지 기다렸다 누름 |
| 5 | 가로 넘침을 innerWidth 에 댐 — 태블릿 흉내에서 넓은 쪽을 줄여 맞추면 innerWidth 도 커져 넘침을 못 봄 | 깨기 F 에서 태블릿만 1,601px 인데 PASS | 화면 폭에 댐. **최종 관문 스윕도 같은 SNAPSHOT(lib/harness.cjs)** → 관문 기록 4,869곳을 다시 세어 넘침 0 · SNAPSHOT 자체 고침은 도구 고침 몫 |

## 한계 (맡은 곳)

- 이용권이 없어 유료 강의 화면은 이번에 직접 열지 않음(같은 화면 코드의 무료 강의로 봄) — 이용권 쓰는 점검을 다시 하려면 사장님이 새 점검용 브라우저에 코드를 넣으셔야 함(사장님).
- 전화 · 블루투스 끊김처럼 밖에서 소리가 멈추는 경우는 흉내 못 냄 — 실제 휴대폰 30분 확인(사장님).
- '다음 강의' 빨리 누르기는 6번 시도 중 2번만 눌림(불러오는 동안은 단추가 없음) — 불러오는 중 누름은 봤음.
- 키보드는 쪽마다 Tab 40번(끝까지는 아님) · 화면 낭독기는 따로 안 봄(9/18 도 같음).
- SNAPSHOT 의 태블릿 넘침 사각 — 다음 스윕 전에 lib/harness.cjs 고침(도구 고침).

## 다시 돌리는 법

```
# 이용권 없는 새 브라우저(빈 폴더를 원본으로)
$env:KIG_PROFILE_SOURCE = "$env:TEMP\kig-empty-profile-0926"   # 빈 폴더
$env:KIG_CLONE_PREFIX = "kig-free-0926-"
node docs/qa-2026-09-18/scripts/drive-common-0926.cjs --free              # A ~ F
node docs/qa-2026-09-18/scripts/drive-common-0926.cjs --free --only D --break D   # 깨기(A ~ F 각각)
node docs/qa-2026-09-18/scripts/check-a11y.cjs --pages <15쪽> --out docs/qa-2026-09-18/out/a11y-0926.json
node docs/qa-2026-09-18/scripts/a11y-compare-0926.cjs
node docs/qa-2026-09-18/scripts/scan-overflow-0926.cjs
node docs/qa-2026-09-18/scripts/diag-reading-gap-stop-0926.cjs [--toggle]   # BUG-034
```

`results/` 의 파일: 첫 판(`common-0926.jsonl` — A 의 STUDENT · D · E 는 도구 흠 1 ~ 3 때문에 r2 · r3 로 대체) · r2(A · D · E) · r3(D) · r4(F) · 깨기 A ~ F ·
접근성(오늘 · 깨기 · 9/18 · 비교 둘) · 관문 기록 넘침 다시 세기(+ 깨기) · BUG-034 재현 셋.
