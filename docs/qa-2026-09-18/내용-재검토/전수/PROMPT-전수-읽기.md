# 학습 내용 전수 읽기 — 9/18 뒤 안 바뀌고 표본에도 안 든 글 전부

## 소유자용 안내

**왜 하나요.** 학습 내용 재검토(관문 15)는 9/18 뒤 바뀐 글 전부와 안 바뀐 글의 표본 86쪽을 읽었습니다. 표본에서 9/18 감사가 놓친 틀림이 나와
(듣기는 표본 28쪽 중 21쪽에서 — 힌트 칩 · 한국어 띄어쓰기, 나머지 과정은 낮음 몇 건), 소유자 결정 2026-09-24 17:1x **"1번2번다 할거야"** —
표본에 안 든 **나머지 글 전부(약 2만 줄 · 6과정)** 를 읽습니다. 워크플로우로 나눠서(소유자 "거기서도 워크플로우가 돌아가게 하자").

**누가 · 언제.** 새 세션 하나. 내용 재검토 세션 · 수정 세션 · 3차 점검 세션이 아닙니다(같은 AI 계열이라 독립 검토는 아님).
최종 출시 관문과 같이 돌아도 됩니다 — 브라우저를 쓰지 않고 내용 파일을 바꾸지 않습니다. 틀림은 수정 세션이 관문 0 뒤에 재검토 결과와 함께 한 번에 고칩니다.

**시작.** 새 세션을 열고(새 작업 트리) 이 한 줄을 붙이세요:

> 먼저 `git merge --no-edit claude/learning-content-review-8d209a` 를 하고, docs/qa-2026-09-18/내용-재검토/전수/PROMPT-전수-읽기.md 의 ==== 사이 명령대로 해. 워크플로우 써서 과정별로 나눠서 해.

====================== 여기서부터 명령 ======================

학습 내용 전수 읽기다. 읽고 판정만 해라. 고치지 마라.

■ 시작 (5분)
  - `git branch --show-current` 가 main 이면 멈추고 소유자에게 "새 작업 트리에서 열어 달라" 고 말하라(본 저장소 폴더에서는 옆 세션이 관문을 돌린다).
  - 가지 claude/learning-content-review-8d209a 를 합친다(`git merge --no-edit claude/learning-content-review-8d209a` — docs/qa-2026-09-18/내용-재검토/ 만 들어옴).
    그 가지에 목록 · 분류 · 표본 · 도구가 있다. 합친 뒤 `git diff --name-only HEAD~1 HEAD -- content src` 가 0 줄인지 확인.
  - `node docs/qa-2026-09-18/내용-재검토/scripts/full-read.cjs` → 전수/읽을거리/<조각>/NN.md · ids.json 을 만든다(4 ~ 8분). 조각 10: student · voca-a · voca-b(VOCA 를 읽을 글 수로 반씩 — 강의 차례) ·
    grammar1-a(gh1-006 ~ 063) · grammar1-b(064 ~ 123) · grammar2 · ld-a(d001 ~ 138) · ld-b(d139 ~ 276) · reading-a(pr001 ~ 128) · reading-b(pr129 ~ 256).
    출력의 조각별 '읽을글' 합이 약 2만(내용 재검토 coverage.cjs '둘 다 아님' 20,134 와 같은 단위 — 1부 판정 · 표본에서 읽은 글은 빠지고, 1부 판정 일꾼이 옆 글에서 이미 적은 틀림은 표시됨)인지 확인해 기록에 적어라.
  - docs/qa-2026-09-18/내용-재검토/전수/기록.md 를 만들고 맨 위에 아래 '기준' 을 그대로 적는다. 단계마다 걸린 시간을 적는다.

■ 지킬 것
  - 내용 파일(content/ · src/lib 의 글 파일) · 코드 · 음성 클립을 바꾸지 마라(옆 세션이 최종 관문을 돌린다 — 한 글자만 바뀌어도 가짜 누락).
  - 브라우저를 열지 마라. 이용권 · 관리자 화면 · /api/ 를 부르지 마라. .env.local 값을 출력하지 마라.
  - 쓰는 곳은 docs/qa-2026-09-18/내용-재검토/전수/ 뿐. git 은 그 폴더의 파일만 이름으로 add · 커밋. git add -A · git add . · git stash 금지. 푸시 · 배포 금지.
  - CNN · GVA · basics · middle 은 보지 마라. 법률 문서 · 결제 · 저작권은 판정 대상이 아니고 보고에 꺼내지도 마라.
  - 소유자가 이미 정한 것은 다시 따지지 마라 — docs/qa-2026-09-18/내용-재검토/소유자-결정-요약.md.
    특히 (2026-09-24 18:1x · 18:2x) 되살린 원본 글의 그 시대 사실(토요일 수업 · 수업 시간 등)은 틀림이 아니라 '원본 그대로(소유자 기준)' — 따로 셈(add-full.cjs "원본 그대로"),
    내용 재검토 결과의 '소유자 답' 1 · 3 · 4(docs/qa-2026-09-18/내용-재검토/결과-머리.md) — 문법 II 에도 '그것 = that' 적용 등.
  - 이 명령을 받기 전에 이미 가지를 합쳤다면 한 번 더 `git merge --no-edit claude/learning-content-review-8d209a` (이 규칙이 뒤에 더해짐).

■ 기준 — 내용 재검토와 같다. 기록 파일 맨 위에 그대로 적고 끝까지 바꾸지 마라
  (docs/qa-2026-09-18/PROMPT-내용-재검토.md 의 '■ 기준' 그대로 + docs/qa-2026-09-18/내용-재검토/기록.md 의 '기준에 붙인 풀이' 1 ~ 5.
   특히 풀이 5 — ③ 채점 심각도는 9/18 감사의 매김: 틀린 답을 만점 = 높음 · 맞는 답 0점 = 중간 · 70점 = 낮음 · 답 열쇠가 모두 문제 뜻을 빠뜨림 = 높음.)

■ 나누기 — 워크플로우 (소유자가 이 세션에서 직접 허락)
  - 스크립트는 만들어 두었다: `Workflow({ scriptPath: "<이 작업 트리>/docs/qa-2026-09-18/내용-재검토/전수/full-read-workflow.js", args: { wt: "<이 작업 트리 절대 경로>" } })`.
    조각 10개 일꾼(add-full.cjs 로만 적음) → 조각마다 다른 일꾼이 확인(verify-full.cjs: 심각 · 높음 · ③ 전부 · 중간 · 낮음 20% · 판단 필요 전부 · 묶음 3%(최소 2)를 판정을 가린 채 통째로 다시 읽기).
    스크립트를 읽어 보고, 이 명령과 어긋나는 곳이 있으면 고쳐서 돌려라(고친 것은 기록에).
  - 일꾼은 전수/판정-<조각>.json · 전수/기록-<조각>.md · 전수/work/ 에만 쓴다. 합치기 · 커밋은 이 세션이 한다.
  - 진행: `node docs/qa-2026-09-18/내용-재검토/scripts/add-full.cjs <조각> --status` · 확인 `verify-full.cjs <조각> --status`.

■ 끝나면 — 합치기 · 조정 · 보고
  - `node docs/qa-2026-09-18/내용-재검토/scripts/full-merge.cjs` → 전수/판정.json · 전수/결과.md.
  - 확인에서 판단 필요가 뒤집힌 것 · 확인이 안 된 심각 · 높음은 이 세션이 읽고 전수/조정.json 에 정한 뒤 다시 합친다(조정의 형식은 full-merge.cjs 맨 위).
  - 전수/결과-머리.md 에 소유자용 머리글: 맨 위 한 줄 🟢 틀림 0 · 🟡 낮음 · 중간만 · 🔴 심각 · 높음 있음 — 쉬운 말로, 과정별 숫자, 소유자가 할 일(판단 필요 번호 답).
    그리고 full-merge.cjs 를 다시 돌려 결과.md 에 넣는다.
  - 틀림 표는 수정 세션이 그대로 고칠 수 있게(파일 · 칸 · 지금 · 고칠 글 · 새 음성 클립). 표 끝에 이 문장을 그대로:
    "이 전수 읽기는 글을 고친 세션과 다른 세션이 했지만 같은 AI 계열입니다. 독립적인 검토가 아닙니다."
  - 전수/ 의 파일을 이름으로 커밋(읽을거리 · work 는 .gitignore). 푸시하지 마라.
  - 끝나면 3차 점검 세션에 결과 가지 이름과 숫자를 알려라(SendMessage — ListAgents 로 '3차 점검').

====================== 여기까지 명령 ======================
