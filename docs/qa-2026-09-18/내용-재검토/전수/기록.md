# 학습 내용 전수 읽기 — 기록

명령서: [`PROMPT-전수-읽기.md`](PROMPT-전수-읽기.md) (==== 사이). 조각별 판정 `판정-<조각>.json` · 확인 `판정-<조각>-확인.json` · 조정 `조정.json` → 합친 것 `판정.json` · `결과.md`.
세션이 압축되면 **이 파일과 `node docs/qa-2026-09-18/내용-재검토/scripts/add-full.cjs <조각> --status` · `verify-full.cjs <조각> --status` 부터** 다시 읽고 이어서 한다. 아래 기준은 끝까지 바꾸지 않는다.

## 기준 (명령서 그대로 — 시작 전에 고정)

기준 = [`../../PROMPT-내용-재검토.md`](../../PROMPT-내용-재검토.md) 의 '■ 기준'(41 ~ 57행) 그대로 + [`../기록.md`](../기록.md) 의 '기준에 붙인 풀이' 1 ~ 5 그대로.

■ 기준 — 시작 전에 고정한다. 기록 파일 맨 위에 그대로 적고 끝까지 바꾸지 마라
  - 기준 판 = 9d6e15d (9/18 감사가 본 운영 main — K-IG_Commercial_Release_Readiness_Report.md 3행).
    지금 판 = HEAD. 운영과 내용이 같아야 한다 — 시작할 때 git diff --name-only a6ff936 HEAD -- content src/lib 의 글 파일이 0 인지 확인해 적어라.
  - 틀림(고쳐야 함):
    ① 영어가 틀림 — 문법 · 낱말 · 철자 · 뜻을 바꾸는 문장부호
    ② 한국어 뜻이 영어와 다름 · 한국어 맞춤법이 틀림
    ③ 정답 · 대체 답이 문제와 안 맞음 — 맞는 답을 막거나 틀린 답을 받음
    ④ 사실이 틀림 — 날짜 · 이름 · 숫자
    ⑤ 소리 내는 글이 화면 글과 다름 · 발음이 화면에 보이는 뜻과 다름
    ⑥ 힌트 · 칩 · 제목 · 설명이 그 문장 · 강의와 안 맞음
    ⑦ 고치다 생긴 흔적 — 잘린 문장 · 겹친 낱말 · 남은 기호 · 두 문장이 붙음 · 한 강의 안에서 같은 글이 서로 다르게 고쳐짐
  - 틀림이 아님: 문체 취향 · 영국/미국 표기(한 강의 안에서 섞이지 않으면) · 원본과 다름 그 자체.
  - 판단 필요: 둘 다 말이 되는데 하나를 골라야 하는 것 · 교육 방침이 걸린 것 → 소유자 결정표로(추천안과 까닭).
  - 심각도(9/18 감사와 같은 넷): 심각 = 틀린 영어나 틀린 뜻을 가르침 · 높음 = 채점이 틀리거나 영어와 한국어가 서로 달라 학습자를 오도
    · 중간 = 헷갈리게 하지만 오도는 아님(오타 · 문장부호) · 낮음 = 띄어쓰기 · 한 강의 안 표기 흔들림.
  - 틀림마다 적을 것: 과정 · 강의 · 파일 · 칸(JSON 경로) · 고치기 전(9d6e15d) · 지금 · 왜 틀렸나 · 고칠 글 · 심각도 · 확신(높음/보통).
    보고 전에 그 강의의 화면 글(expectations)에서 학습자가 그 글을 실제로 보거나 듣는지 확인해라 — 안 닿는 칸이면 '화면에 안 닿음' 으로 따로.

### 기준에 붙인 풀이 — 같은 종류를 두 가지로 다루지 않도록 (정할 때마다 여기에 한 줄씩 더함)

1. 소유자가 이미 정한 것(소유자-결정표-6단계.md 22개 · 7단계 7-4 f · 2026-09-24 아침 답 · bug-register.md 소유자 결정 · "낱말 소리는 화면에 보이는 뜻의 발음")은
   다시 따지지 않는다. 결정대로 들어갔는지만 본다. 결정이 분명히 틀렸다고 보이면 틀림으로 세지 않고 '결정과 다름' 칸에 따로 적는다.
2. 원본 교재와 다르다는 것만으로는 틀림이 아니다. 원본은 망가진 글(옮기다 깨진 것)을 찾는 데만 쓴다.
3. '닿음' 은 학습자가 그 글을 **보거나 · 듣거나 · 그 글로 채점받는** 것. 화면 코드가 그 칸을 그리는지로 정한다(아래 '닿음 규칙').
4. 한 글 조각에 틀림이 둘이면 더 무거운 쪽 하나로 세고, 까닭에 둘 다 적는다.
5. **③ 채점의 심각도 — 9/18 감사의 매김에 맞춤**(기준 '심각도(9/18 감사와 같은 넷)'):
   - **틀린 답(틀린 영어 · 문제와 뜻이 다른 영어)을 만점으로 받음** — 화면 '다른 정답' 에도 보여 틀린 것을 맞다고 가르침 → **높음**.
   - **맞는 답을 막음**(흔한 맞는 번역이 대체 답안에 없어 0점) → **중간**. 9/18 감사가 이 종류(missing-alternative)를 그렇게 매김 —
     6단계 목록(9/18 의 중간 · 낮음 1,759)의 missing-alternative GRAMMAR I 중간 74 · 낮음 35 · GRAMMAR II 중간 8 · 낮음 3 · READING 중간 16 · 낮음 8 · LISTENING 중간 9 · 낮음 5
     (예: 6-1327 "It's 7 o'clock." 0점 = Medium · 6-1338 "Which one do you want?" 0점 = Medium). 막힌 답이 **70점**이면 **낮음**.
   - 모범 답안과 대체 답안이 **모두** 한국어 문제의 뜻을 빠뜨려 가장 곧은 번역이 막히면(9/18 이 High 로 매긴 gh1-050/051 '지금' 처럼) 답 열쇠가 문제와 안 맞는 것 → **높음**.
   - 소유자가 70점으로 정한 꼴('그 N' 280곳 · 꼬리 질문만 틀린 답 등)은 틀림 아님.
   - 이력: 16:08 에 처음엔 '0점 = 높음' 으로 적었다가, grammar1-a 일꾼이 든 6단계 관례(6-1327 · 6-1338 · 6-1342)를 원문으로 확인하고 16:2x 에 위처럼 바꿈.
     그 사이 이 규칙으로 판정한 줄은 없음(규칙은 합칠 때 적용 — 일꾼 심각도와 다르면 합칠 때 바꾸고 까닭을 남김). 16:08 에 바꾼 확인 도구의 **'③ 채점 전부 확인'** 은 그대로 둠
     (조각마다 매김이 달라 ③ 을 모두 두 번 보는 편이 맞음 — 어느 조각도 확인 대상을 뽑기 전에 바꿈).

(위 두 덩이는 원문에서 글자 그대로 옮김 — '■ 기준' 은 명령서 41 ~ 57행 1,055자와 같음을 스크래치 `cmp-standards.cjs` 로 확인. 워크플로우 스크립트의 STANDARDS 도 같은 1,055자.
 일꾼에게는 이 기준과 함께 전수용 풀이 1 ~ 9(`full-read-workflow.js` NOTES — 위 풀이 1 ~ 5 를 전수 읽기에 맞게 옮기고 6 ~ 9 를 더함: 문체 취향은 틀림 아님 · '고칠 글' 은 칸 전체 · '새 음성 클립' 정하는 법 · 되풀이 틀림은 T 마다)를 넘김.
 전수에서 달라진 한 가지: 판정 도구 add-full.cjs 에 '결정과 다름' 칸이 없어 풀이 1 의 '결정과 다름' 은 '판단 필요' 에 "결정과 다름: …" 으로 적음.)

## 시작 확인 (2026-09-24)

| 확인 | 결과 |
|---|---|
| 가지 | `claude/content-review-merge-process-0a9081` (main 아님) · 새 작업 트리 `hopeful-joliot-c445c0` |
| 합치기 | 17:36 `git merge --no-edit claude/learning-content-review-8d209a`(`ce258fc`) → `a9e981d` · 들어온 것 66파일 모두 `docs/qa-2026-09-18/내용-재검토/` · `git diff --name-only HEAD~1 HEAD -- content src` **0줄** |
| 기준 판 · 지금 판 | 기준 `9d6e15d` · 지금 HEAD `a9e981d`. `git diff --name-only a6ff936 HEAD -- content src/lib` → `src/lib/license.ts` · `licenseSession.ts` · `serverLicense.ts` 셋(이용권 코드)뿐 = **글 파일 0** |
| 읽을거리가 쓰는 판 | 도구(lib.cjs)의 HEAD = `2a80bba`. `git diff --name-only 2a80bba HEAD -- content src/lib` **0** · `git diff --name-only HEAD main -- content src/lib`(main = `2d388aa`) **0** → 읽을거리의 글 = 이 작업 트리 = main |
| node_modules | 이 작업 트리에 없어 17:36 `full-read.cjs` 가 `node_modules/typescript` 를 못 찾고 멈춤(lib.cjs 가 tsload 의 REPO 를 이 작업 트리로 바꾸므로 이 작업 트리의 것을 찾음). → `pnpm install --frozen-lockfile --offline --ignore-scripts`(내려받기 없음 · 2분 19초). `/node_modules` 는 .gitignore — 내용 · 코드 파일 변화 0(`git status` 깨끗) |
| 읽을거리 | 17:40:37 ~ 17:46:56 (**6분 19초**) `node docs/qa-2026-09-18/내용-재검토/scripts/full-read.cjs` → `전수/읽을거리/<조각>/NN.md` · `ids.json` — 조각 10 · 파일 56 |

### 읽을 글 — 과정별 합이 내용 재검토 coverage.cjs '둘 다 아님' 과 같은가

| 과정 | 묶음 | 읽을글(full-read.cjs) | coverage '둘 다 아님' | 같음 |
|---|---|---|---|---|
| STUDENT | 76 | 576 | 576 | ✔ |
| VOCA | 184 | 6,704 | 6,704 | ✔ |
| GRAMMAR I | 48 | 2,244 | 2,244 | ✔ |
| GRAMMAR II | 38 | 1,135 | 1,135 | ✔ |
| LISTENING | 261 | 3,893 | 3,893 | ✔ |
| READING | 243 | 5,582 | 5,582 | ✔ |
| 합 | **850** | **20,134** | **20,134** | ✔ |

조각별: student 576(묶음 76 · 파일 3) · voca-a 3,400(64 · 3) · voca-b 3,304(120 · 7) · grammar1-a 1,239(24 · 4) · grammar1-b 1,005(24 · 4) · grammar2 1,135(38 · 4) ·
ld-a 2,168(131 · 9) · ld-b 1,725(130 · 8) · reading-a 2,861(123 · 7) · reading-b 2,721(120 · 7) = 20,134.

## 워크플로우 스크립트 검토 — 명령과 대조 (17:47 ~)

- 맞음: 조각 10(명령서 차례의 이름과 범위 그대로) · 읽기 일꾼은 add-full.cjs 로만 적음 · 확인 대상 = verify-full.cjs `--pick`(심각 · 높음 · ③ 전부 · 중간 · 낮음 20% 해시 · 판단 필요 전부 · 묶음 3% 최소 2) ·
  일꾼이 쓰는 곳 = 전수/판정-<조각>(-확인).json · 기록-<조각>(-확인).md · work/ · 저장소 밖 스크래치 · git · 브라우저 · /api/ 안 부름 · STANDARDS 글자 그대로(위).
- 이 컴퓨터에서 동시에 도는 일꾼은 6(워크플로우 상한 = CPU 8 − 2). 일꾼 20(읽기 10 + 확인 10)은 자리가 나는 대로 이어 돎.
- **고친 것 1 — 확인 일꾼의 차례(명령과 어긋남)**: 스크립트는 ① `--pick` 전체와 확인.json '대상'(판정 일꾼의 주장)을 읽고 → ② F: · D: → ③ G:(통째로 다시 읽기) 차례였다.
  G: 로 뽑힌 묶음에 F: · D: 대상이 있으면 확인 일꾼이 판정 일꾼의 주장을 먼저 보게 되어 명령의 **"판정을 가린 채 통째로 다시 읽기"** 와 어긋난다.
  → G: 를 맨 먼저: `--pick` 출력에서 G: 줄만 보고(주장 · 판정 파일은 열지 않음) G: 결과를 다 적은 뒤에 F: · D: 로.
- **고친 것 2 — GRAMMAR 채점 재기를 한 도구로**: 풀이 5 는 0점 · 70점을 앱 채점 함수로 재라고 하는데, 스크립트는 일꾼마다 제 스크래치 도구를 만들게 두었다(1부에서 조각마다 grade.cjs · g.cjs 가 따로 생김).
  조각 셋(grammar1-a · 1-b · 2)과 확인 셋이 같게 재도록 저장소 밖 스크래치패드에 `grade.cjs` 하나를 두고 일꾼에게 넘김:
  앱과 같은 참조(`[정답, ...다른 정답]` 을 앱의 cleanText 뒤에 — GrammarLearningView.tsx 60 · 222 · 474) · 읽을거리와 같은 짝 함수(render.cjs grammarPair · expectations pairOf) ·
  **시작마다 도구 확인**(같은 글 → 만점 · 다른 낱말 → 0점 · 기능어 하나 더 → 70점 — 다르면 exit 3) · **문항마다 찾기 확인**(정답 자체 → 만점 ✔).
  시험(17:5x): gh2-011 #1 정답 "Whom do you live with?" 찾기 확인 ✔ · "banana banana" 0점 · 참조 "It was 7 o'clock then." 에 "It was seven o'clock then." 0점 / 마침표만 뺀 꼴 만점.
- **고친 것 3 — LISTENING 칩 재기 도구**: 방법 4 '고칠 글이 새 틀림을 만들지 않는지' 를 칩에서 잴 수 있게 `chips.cjs dNNN [고칠 힌트 줄.txt]` — 앱 규칙(expectations hintChunks · hintsForSentence)으로
  문장마다 지금 칩 → 고친 뒤 칩(달라진 문장 ◆). 도구 확인: "Mrs. Watson. 4,000 dollars" → [Mrs] [Watson] [4,000 dollars](약어 마침표가 경계로 읽힘 · 천 단위 쉼표는 안 자름).
  시험: d010 지금 칩 [French Portuguese] · [Lake Victoria Nile River] — 두 항목이 한 칩으로 붙은 꼴이 그대로 보임.
- 두 도구는 읽기만 한다(저장소 파일을 쓰지 않음). 일꾼이 쓰는 입력 파일도 저장소 밖(스크래치패드 `cases/<조각>/`).

## 단계 · 걸린 시간

| 시각 | 무엇 | 걸린 시간 |
|---|---|---|
| 17:36 | 합치기 · 확인(content · src 0줄) | 1분 |
| 17:36 ~ 17:40 | full-read.cjs 첫 시도 멈춤(node_modules 없음) → pnpm 오프라인 설치 | 4분 |
| 17:40 ~ 17:47 | 읽을거리 만들기(full-read.cjs) — 20,134 = coverage 와 같음 | 6분 19초 |
| 17:47 ~ | 명령서 · 결정 요약 · 도구 · 워크플로우 스크립트 읽기 · 도구 두 개 · 이 기록 | |
