# 최종 출시 관문 — 작업기록 (2026-09-24~)

명령: `PROMPT-최종-출시관문.md` 의 `====` 사이(7e0abf6 판 — 관문 15 포함). 소유자 지시(이 채팅): "docs/qa-2026-09-18/PROMPT-최종-출시관문.md 의 ==== 사이 명령대로 최종 출시 관문을 돌려".
**이 점검은 수정을 한 세션이 직접 수행한다 — 독립적인 검토가 아니다.** 컨텍스트가 줄면 이 파일의 '기준' 과 '멈춘 곳' 을 먼저 다시 읽는다.

## 기준 (명령서에서 — 바꾸지 않음)
- 고치지 마라 · 확인만 · 문제를 찾으면 보고하고 멈춤(단, 관문 15 의 내용 틀림은 관문 0 뒤 한 번 고침). 하나 막혀도 끝까지 다 확인.
- 운영(https://k-ig-core.vercel.app)에서 확인 — 로컬 결과는 근거가 아님(도구 증명만 로컬).
- 감사용 프로필(원본 %TEMP%\kig-audit-licensed-profile — 지우지 않음, 복제본만 씀) · 새 기기 등록 0 · 코드 · 비밀번호 · PIN 타이핑 0 ·
  /api/license/ 상태 바꾸는 호출 0 · /api/admin/ 변경 계열 0 · CNN · GVA 안 건드림 · 법률 · 결제 · 저작권은 대상 아님.
- 브라우저 작업 2~3개까지 · 속도 측정(관문 12)은 단독.
- **마지막 배포**: 앱이 마지막으로 바뀐 배포 `0524168`(14:21:41) · 그 뒤 문서 · 도구만 `e5110a5`(GitHub Production success **2026-09-24T06:09:22Z = 15:09:22**).
  관문 0 기록은 이 배포 뒤 것만. 관문 도는 동안 푸시 안 함(관문 15 의 소유자 허락 배포 말고).
- 관문 0 이 도는 동안 content/ · src/lib 글 파일 안 바꿈.
- 순서: 관문 0(도구 증명 → 전수 스윕) → 관문 15(재검토 결과 받기 → 틀림 고침 → 소유자 허락 배포 → 달라진 강의 다시) → 관문 1~14 → 보고.
- 보고: 관문마다 "관문 N — 통과 / 불통과, 근거 숫자" · 불통과를 맨 위에 · 끝에 "이 점검은 수정을 한 세션이 직접 수행했습니다. 독립적인 검토가 아닙니다."

## 진행

### 관문 0-a · 도구가 실패를 잡는지 다시 증명 (proof-summary 12 시험, 지금 코드)
- 방법(4-5단계-작업기록 §3 과 같음): 로컬 개발 서버(kig-dev 3211)에서 시험 12개의 깨뜨린 지점을 한꺼번에 `PROOF-BREAK` 표시로 깨뜨림 → `proof-run.cjs --phase g0-fixed`
  (드라이버 기본값 그대로 · 3화면) → 되돌림(`git diff -- src` 비어 있음 · PROOF-BREAK 0) → `--phase g0-after` → `proof-summary.cjs --md --prefix g0-`
  (새 검사 판정: 깨진 앱에서 3화면 모두 없앤 글자를 '없음' · 되돌린 앱에서 3화면 모두 '있음' = 실패 잡음 확인). 옛 단계 기록은 덮지 않음(단계 이름 g0-).
- **실제로 한 방법(15:1x~)**: 함께 쓰는 작업 폴더를 건드리지 않으려고(3차 점검 · 재검토 세션이 같은 폴더) **따로 떼어 낸 작업 트리**
  `%TEMP%\kig-wt-proof`(HEAD 2a80bba · pnpm 오프라인 · public/audio 는 저장소 것으로 이음)에서만 깨뜨림 — 9곳(시험 12개: GRAMMAR 두 자리가 I · II 에,
  LISTENING 한 자리가 1번 · 마지막 문장에): PhonicsLearningView 낱말표 'about' · GrammarLearningView STEP 1 마지막 문항 한국어 · Step 3 마지막 문항 영어 ·
  StudentLearningView STEP 1 마지막 문장 · ReadingLearningView STEP 1 마지막 문장 · STEP 2 마지막 카드 낱말 · 뜻 · STEP 4 한글 마지막 문장 ·
  LdLearningView 대역 스크립트 1번 · 마지막 문장 한국어 — 모두 `PROOF-BREAK g0` 표시. 저장소 `src` 변경 0(확인함).
  그 트리의 `next dev`(3211, launch.json `kig-proof-wt`)에 `proof-run.cjs --phase g0-fixed` 셋을 나눠 동시에(브라우저 3):
  ① phonics:mv1-01 · grammar1:gh1-007 · gh1-006 ② grammar2:gh2-007 · gh2-007-1 · student:s1-1 ③ reading:pr001 · pr001-1 · ld:d001-1.
- `proof-summary.cjs` 에 `--prefix` 를 더함(한꺼번에 깨뜨린 새 묶음을 제 단계 이름으로 읽음 — 9/23 기록은 그대로).
- **결과(15:57) — `proof-summary.cjs --md --prefix g0-`: 12개 시험 모두 '실패 잡음 확인'** (깨진 앱에서 3화면 모두 없앤 글자를 없음으로 · 되돌린 앱에서 3화면 모두 다 있음):
  VOCA mv1-01 29/30✗ → 30/30 · GRAMMAR I gh1-007 · gh1-006 41/42✗ → 42/42 · GRAMMAR II gh2-007 · gh2-007-1 15/16✗ → 16/16 · STUDENT s1-1 5/6✗ → 6/6 ·
  READING pr001(지문 마지막 문장 · 어휘 낱말 · 뜻 — 셋을 같이 깨뜨려 30/33✗) → 33/33 · pr001-1(한글 마지막 문장 · 같은 쪽의 어휘 둘도 같이 깨짐 21/24✗) → 24/24 ·
  LISTENING d001-1(1번 · 마지막 문장 한국어 4/6✗) → 6/6. 9/23 표(접두 없이)도 그대로 12개 '실패 잡음 확인'. 표 전체: 스크래치 `proof-g0-table.md`.
- **증명 중 찾은 도구 결함 둘(고침)**: ① `proof-run.cjs` 가 모든 강의에 `--redo` 를 줘, 한 단계에서 같은 과정 둘째 강의가 첫째 강의 기록을 옆 파일로 치움
  (7-1 l 의 --redo 뜻 뒤로 생긴 것) → 과정마다 첫 강의만 --redo, 다음은 --resume. 이번 실행에서 치워진 3묶음(gh1-007 · gh2-007 · pr001, 두 단계씩)은 같은 실행의
  진짜 기록이라 제자리 파일에 다시 붙임(치운 파일은 그대로 둠 — 스크래치 `merge-proof-aside.cjs`). ② `--prefix` 묶음은 시험마다의 옛 단계 이름(`r2-`)을 쓰지 않게.
- 치움: 증명 서버 끔 · 떼어 낸 작업 트리 지움(소리 폴더 연결은 먼저 `rmdir` 로 연결만 끊음 — 저장소 소리 52,409 그대로) · launch.json 되돌림.

### 관문 0-b · 운영 전수 스윕 계획
- 잰 속도(최근 기록 — 과정 × 화면별 방문 한 번의 가운데 값): 합 약 61 일꾼-시간 → **브라우저 3개로 약 20시간**. 가장 긴 것: LISTENING 데스크톱 10.8h ·
  태블릿 10.3h · GRAMMAR I 태블릿 9.1h · GRAMMAR II 태블릿 5.8h · STUDENT 태블릿 5.5h · 휴대폰 4.5h.
- 스크래치 `gate0-sweep.cjs`: 과정 × 화면을 2시간 안팎 조각(`--shard`)으로 39개 · 긴 것부터 빈 일꾼 셋이 가져감 · 조각마다 기록 파일
  `out/features/<과정>-g0-<화면>-<i>of<n>.jsonl` · 끊기면 `--resume`(같은 파일만 — 옛 기록 안 섞음) · 한 조각 3번까지 다시 · 상태 `gate0-state.json`.
  드라이버는 7단계 고친 판 그대로(`drive-generic.cjs` DRIVER_REV 7-1m — 칸별 읽개 · 서버 오류 기록 보존 · 받아쓰기 조립 · STUDENT 3단계 완료 · 휴대폰 타일).

### 관문 15 준비 (3차 점검 알림)
- 학습 내용 재검토 세션: 워크플로우(조각 9 · 동시 6 · 소유자 허락) · 브라우저 0 · 내용 파일 안 씀. 작업 트리 `.claude/worktrees/nostalgic-blackburn-048c73`,
  가지 `claude/learning-content-review-8d209a`(2a80bba 에서 갈라짐). **결과는 그 가지에서 읽는다**:
  `git show claude/learning-content-review-8d209a:docs/qa-2026-09-18/내용-재검토-결과.md`. 관문 뒤 푸시 전에 그 가지의 문서
  (`docs/qa-2026-09-18/내용-재검토/**` · 결과 md)를 main 에 합친다. 준비 숫자: 목록 15,128줄 · 파일 1,149(= git diff) · 닿음 5,652 · 표본 86쪽.

- **시작에서 잡은 것(16:10)**: 첫 실행이 39조각 모두 1초 만에 exit 0 — drive-generic 의 `--resume` 이 같은 과정의 옛 전수 기록(`<과정>.jsonl` ·
  `<과정>-s*.jsonl`, 9/18 등)까지 '이미 함' 으로 세어(예: STUDENT '다른 기록에서 246') 할 일이 0 이었음. **옛 기록은 한 줄도 쓰이지 않음**(새 기록 0건 — 확인).
  → 순서표가 `--resume` 을 안 쓰게 고침: 첫 실행은 새 파일 · 끊긴 조각은 그 조각 파일에서 본 강의를 뺀 나머지를 `--ids` 로 새 이름(-r<k>) 파일에.
  미리 보기로 39조각 = 4,869방문 = 전 쪽 × 3화면 확인. (드라이버의 --resume 함정은 관문 보고에 적음 — 관문 중에는 드라이버를 고치지 않음.)
- **관문 0 스윕 시작 2026-09-24T07:12:45Z(16:12:45)** — 마지막 배포(15:09:22) 뒤. 절전 막기 요청(세션이 쉬면 풀림 — 확인하며 기다림).
  첫 1분 반: 3일꾼 모두 방문 중(STUDENT 데스크톱 · VOCA 태블릿 · GRAMMAR II 태블릿). 태블릿이 잰 값보다 훨씬 빠름(쪽마다 몇 초) — 전체가 20시간보다 짧을 수 있음.

### 스윕 도는 동안 준비한 것 (17:2x~ · 브라우저 추가 0)
- 스크래치 `gate-count.cjs`: 관문 0 기록 파일만(`<과정>-g0|g15-<화면>-<i>of<n>(-r<k>).jsonl` — 옛 기록은 이름 규칙으로 안 읽음), 강의 × 화면마다 가장 늦은 기록 하나로
  관문 0(파일마다 만든 때 · 첫/마지막 기록 · 배포 전 기록 수 · 운영 아닌 기록) · 전 쪽 × 3화면 · 관문 5(누른 횟수 · 상태) · 관문 9(요청 기록 있는 쪽 · 4xx · 5xx · 연결 실패 · 예외 · 콘솔) ·
  관문 10(기대 · 있음 · 없음 + 목록). 17:30 중간값(630 강의×화면): 배포 전 기록 0 · 운영 아닌 것 0 · 4xx 0 · 5xx 0 · 요청 기록 없는 쪽 0 · 글자 기대 13,827 / 있음 13,827 / 없음 0 · 음성 PASS 1,577 · RETEST 308.
- 관문 9 도구 한계(보고에 적음): 기록은 쪽마다 4xx·5xx 를 **10개까지**, `/_vercel/` · favicon 은 **빼고** 남긴다(lib/harness.cjs 362). 10개에 닿은 쪽 수를 따로 셈.
- 스크래치 `gen-recheck-targets.cjs`: recheck-audio.cjs 는 `--from` 없이 돌면 out/features 의 **옛 기록 전부**와 옛 recheck-audio*.jsonl 을 섞는다 → 관문 5 는
  관문 0 기록에서 대상을 뽑아 `--from` 으로 넘김(그때 도구는 `recheck-audio<suffix>.jsonl` 만 '이미 함'). 거르개는 도구 소스(NOT_PLAYBACK · worthRechecking)를 그대로 읽어 씀.
- **도구 결함 찾음(관문 중이라 안 고침 · 보고에 적음)**: recheck-audio 의 뺌 규칙(마이크 · 이동 · 속도)을 '단계 이름 ▶ 버튼 이름' 전체에 대어, LISTENING STEP 4
  '섀도잉 & **발음 테스트**' 단계의 진짜 재생 버튼 '🔊 소리 듣기'(LdLearningView 1187 `playText(currentShadowSentence.en)`)를 마이크 버튼으로 빼 버림 — 다시 보지 않고
  '해당 없음' 이 됨. 17:30 까지 LISTENING 데스크톱 8강(d016 · d034 · d040 …)에서 이 버튼이 RETEST('10초 안에 소리 요청 없음'). 대상 뽑기에서 '뺀 규칙이 버튼 이름에는 안 맞고
  버튼 이름이 재생 버튼' 인 것만 다시 봄으로 되살려 표시(`titleOnlyMic`). 같은 꼴로 STEP 5 '1.5**배속** 청취' 의 재생 버튼도 빠질 수 있음(지금까지는 속도 버튼만 RETEST).
- d073 STEP 3 연음 클리닉의 '문장 목록'(#1 … #2 …)을 드라이버가 음성 버튼으로 잘못 집은 1건 — 도구 거르개가 '재생 버튼 아님' 으로 뺌(다른 강의에선 안 집힘 · 🔊 버튼 156 PASS).

### 관문 15 재검토 진행 (17:2x 에 본 것 — 결과 파일은 아직 가지에 커밋 안 됨)
- 작업 트리의 초안 `내용-재검토-결과.md`: 판정 1,606 / 5,652 · 틀림 11건(중간 1 · 낮음 10) · 확인 대기 5(GRAMMAR II 채점 — 흔한 영작이 0점) · 표본 '읽음 0' ·
  범위 밖 코드 글 K009(LdLearningView 794 '✍️ 고유 명사 · 숫자 참조' — 중간 · 확인 전). 17:25 에 '전수 읽기' 워크플로우 파일을 새로 씀 — 아직 도는 중.
- **미리 볼 것**: K009 를 고치면 src 컴포넌트가 바뀐 배포 → 명령서대로 관문 0 **전부 다시**(약 8~10시간). 내용만 고치면 달라진 강의만. 결과가 오면 소유자에게 물을 것.
- **17:4x 3차 점검 알림 — 관문 15 가 넓어짐**(명령서 2d388aa, 3차 점검이 로컬 main 에 커밋 · 푸시 안 함): 소유자 결정(재검토 창 17:1x "1번2번다 할거야") — 표본 밖 안 바뀐 글
  전부(20,134줄 · 6과정)를 또 다른 새 세션이 전수 읽기(약 5시간). **두 결과(재검토 가지 8d209a · 전수 읽기 가지 — 이름은 3차 점검이 알림)를 모두 받은 뒤 한 번에 고침.**
  '맞는 답인데 0점' 은 심각도와 상관없이 조건 8 — 남으면 관문 4 도 불통과. 3차 점검이 전한 재검토 숫자: 틀림 약 600 · 심각 0 · 높음 4.
- 17:4x 재검토 초안 판정.json(작업 트리 · 커밋 전)에서 센 것: 틀림 526(높음 7 · 중간 361 · 낮음 158) · 판단 필요 7 · 새 클립 필요 30 · 모두 '고칠 글' 있음.
  종류: ⑥ 힌트 · 칩 306(전부 LISTENING hints) · ② 한국어 148 · ③ 채점 50 · ⑦ 9 · ① 6 · ④ 4 · ⑤ 3.
  **⑥ 306 중 약 108 은 '힌트 글로는 못 고침 — 코드만'**(LdLearningView hintChunks 가 '. ' 에서 나눠 'Mrs.' · 'Dr.' · 'N.' 뒤에서 칩이 갈림) → 고치면 src 컴포넌트가 바뀐 배포
  → 명령서대로 **관문 0 전부 다시**. (검사 복제 lib/expectations.cjs 도 같이 — ld-chip-app-vs-lib.cjs 가 앱 대 복제를 비교.)
- **계획(효율)**: 스윕은 명령서 순서대로 계속(지금 배포의 결함을 일찍 찾는 값 · 결과가 오기 전이라 고칠 것도 없음 · 옛 기록 안 섞임). 고칠 준비는 **따로 떼어 낸 작업 트리**에서
  (관문 0 이 읽는 본 폴더 content/ · src/lib 는 안 건드림 — '관문 0 도는 동안 내용 파일 안 바꿈' 의 까닭(기대 글이 틀어짐)이 생기지 않음). 고친 배포가 스윕 끝나기 전에
  준비되고 src 가 바뀌면 지금 스윕은 그때 멈춤(어차피 전부 다시). 적용 도구: 판정마다 '지금' 글이 그 강의 파일의 한 칸과 정확히 같을 때만 '고칠 글' 로 바꾸고,
  없거나 여럿이면 멈추고 목록으로(추측으로 안 고침).

### 17:50 순서표를 바꿈 — LISTENING · VOCA 는 표본만 (3차 점검이 관문 0 규칙을 과정 단위로 좁힘)
- 3차 점검 알림(17:4x): '코드가 바뀌면 전부 다시' → **그 코드를 쓰는 과정만 전부 다시**(import 를 따라 과정 목록). 잰 것: LdLearningView → ld 만 · vocaUtils →
  PhonicsLearningView(VOCA)만 · listeningUtils → ld · student · page.tsx. 권고: 고친 배포 뒤 어차피 전부 다시 돌 LISTENING(힌트 칩 코드) · VOCA(동의어 SYNONYM_GROUPS)는
  지금 스윕에서 표본만, STUDENT · GRAMMAR I · II · READING 은 끝까지. 명령서에 이 허용을 적는다고 함. **따름** — 규칙이 맞고(그 과정 쪽만 그 코드를 씀 — 고칠 때 import 를
  내가 다시 따라 확인) LISTENING 데스크톱 · 태블릿만 약 20 일꾼-시간을 두 번 돌지 않음. 3차 점검이 적은 메모리: 7.1/7.9GB — 넷째 브라우저 안 엶.
- 17:50 첫 순서표를 멈춤: 드라이버 셋이 '저장 유지 시험'(10강마다 · 완료 누름 → 되돌림) 강의 **사이**일 때(STUDENT 26강째 · LISTENING 39강째 — 다음 시험은 30 · 40강)
  주 프로세스 · 드라이버 · Edge 를 끔 → 남은 프로세스 0. 첫 순서표 기록: VOCA 태블릿 195 · GRAMMAR I 태블릿 194 · GRAMMAR II 태블릿 88 · STUDENT 태블릿 82 ·
  STUDENT 데스크톱 26 · LISTENING 데스크톱 78(1of6 · 2of6 각 39 — 표본으로 씀).
- **17:51 둘째 순서표 시작**(스크래치 `gate0-sweep2.cjs` · 상태 `gate0-state2.json`): 16조각 2,402방문 — STUDENT 데스크톱 남은 56(-r2 … , `--ids`) · 휴대폰 82 ·
  GRAMMAR I 데스크톱 · 휴대폰 194×2 · GRAMMAR II 88×2 · READING 512×3 · 표본: LISTENING 태블릿 · 휴대폰 각 43(13쪽마다 — 홀수라 본 쪽 22 · 대본 쪽 21 섞임) ·
  VOCA 데스크톱 · 휴대폰 각 39(5쪽마다). 끝나면: STUDENT · GRAMMAR I · II · READING 은 전 쪽 × 3화면, LISTENING · VOCA 는 표본(고친 배포 뒤 전부).
  미리 보기로 16조각 = 2,402 = 남은 쪽 합 확인. STUDENT 데스크톱은 나누지 않음(한 이용권의 STUDENT 진도를 두 브라우저가 같이 쓰면 저장이 겹칠 수 있음).

### 관문 15 — 소유자 결정(3차 점검이 전함 · 고칠 때 씀)
- 재검토 창 "2번은 변경하지 말고 나머지는 추천대로 해": s11-1 토요일 수업은 원본 그대로 · gh2-021 #13 대체 답 뺌 · gh1-097 #79 · 81 · 83 모범을 Not every / Not all 로
  (옛 모범은 대체 답으로 · 새 음성 3) · 결정 3(그것 = that)을 GRAMMAR II 에도(gh2-027 #13 부터, G9 방법으로 목록).
- 3차 점검 창 "그대로해": s9-1 #10 '약 45분'(재검토 #170 · #171)도 원본 그대로 — 고치지 않음. 재검토 세션이 결과 문서를 고쳐 둠(틀림 246 → 244).
- 기준: 되살린 원본이 그 책의 시대에 맞는 사실이면 틀림이 아님. 영어 · 정답 · 번역 · 맞춤법 잘못은 그대로 고침.
- 적용 도구 `scripts/apply-content-fixes.cjs`(새 · 미리 보기 기본): 재검토 초안으로 미리 봄 — 표 697 중 자동 592 · 손으로 105(대체 답 배열 · 코드 규칙 · '지금' 이
  설명 글인 LISTENING 표본 칩 등) · 사본에 옛 글 남음 1. 결과가 가지에 커밋되면 그것으로 다시.

## 멈춘 곳 · 다음
- 17:51 둘째 순서표 도는 중(스크래치 `gate0-state2.json` · `gate0-w1~3.log` · 지켜보기 `gate0-watch.cjs`). 끝나면: 기록 날짜 확인 · 관문 5 음성 재검사 ·
  관문 15(두 결과) · 관문 1~14.
