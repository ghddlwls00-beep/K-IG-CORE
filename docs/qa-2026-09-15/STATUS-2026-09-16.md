# 전체 점검 — 2026-09-16 마감 시점

> 아카이브 도착 전, **지금까지 한 것 / 안 한 것 / 빠뜨린 것**을 전부 확인한 결과입니다.
> 각 항목은 코드·운영 사이트에서 실제로 확인했습니다.

---

## 1. 오늘 끝난 것

| 항목 | 커밋 | 검증 |
|---|---|---|
| GVA 섹션 폐지 (사이트 + R2) | `276630e` | 라우트 404, 사이트맵 0건, R2 삭제 확인 |
| 미디어 게이트 + 버킷 비공개 | `91cc15b`…`4d7a4f7` | 잠금 403 / 무료 200 / Range 206, 버킷 직통 401 |
| 미디어 오류 1년 캐시 (RE-015) | `db397d9` | 없는 클립 404 + `no-store` |
| "원어민 음성" 표기 27곳 (KIG-009) | `0335ab0` | 화면에서 문구 변경 확인 |
| Azure 클립 4,596개 생성·업로드 | `bbae053`·`1c2f519` | `pending: 0` |
| 구글 비공식 TTS 제거 | `72048dd` | 브라우저에서 폴백 0회 |
| VOCA 연어 26건 무음 (RE-019) | `1c2f519` | 클립 없음 0/3877 |
| GRAMMAR 채점 LCS (KIG-005) | `cc2eee9` | 운영에서 `a`→0점, 모범답안→98점 |
| canonical 페이지별 (RE-011) | `fd5acf0` | 운영 `<link rel="canonical" href=".../ld/d001">` |
| OG 태그 (RE-012) | `fd5acf0` | 운영 `og:image` 확인 |
| 검색 색인 STUDENT (RE-009) | `00c5df0` | 944 → 1,025 (student 81) |
| 진도 API 챕터 검증 (RE-010) | `00c5df0` | `studentProgress.ts:230` |
| KIG-006 괄호 분리 엔진 | `f86d275`…`ca3a7ba` | 데이터 미기록, 스크립트로 보관 |

**실제 학습 흐름**: 소유자가 직접 확인 + 제가 브라우저로 재확인 (음성 4개 과정 Ava 재생,
채점 정확, 잠금 시 본문 유출 0).

---

## 2. 남은 코드 작업 (아카이브 무관 — 지금 가능)

| 항목 | 상태 | 영향 |
|---|---|---|
| **RE-016** 오류 화면 4종 | ❌ `not-found` `error` `global-error` `loading` 전부 없음 | 없는 주소로 들어오면 안내도 복귀 동선도 없음 |
| **RE-006** CSP 헤더 | ❌ 미수정 | 다른 보안 헤더 5종은 있는데 이것만 빠짐 |
| **RE-008** 사이트맵 1,750 URL | ❌ 미수정 | 잠긴 레슨이 크롤러에 거의 같은 페이지로 보임 |
| **RE-004** 레거시 음원 1,463개 | 🟡 부분 | `unclaimed → 허용` 기본값. 인증 없이 받아짐 |
| **RE-005** VOCA 괄호 TTS 14건 | 🟡 부분 | `colo(u)r` 등이 괄호째 합성됨 |
| **RE-014** 제목 계층 | ❌ | 홈 h1 0개 · `/ld/d001` h1 **3개** |

---

## 3. 🔴 한 번도 보지 않은 영역 — 오늘 처음 확인

### 3-1. 자동 테스트가 없습니다

```
테스트 프레임워크 : ❌ 없음 (vitest/jest/playwright 전부)
테스트 파일       : 0개
package.json      : dev, build, lint, test:reading, test:voca, test:audio …
```

`test:*` 세 개는 데이터 검사 스크립트이고 **코드 회귀를 잡는 테스트가 아닙니다.**

오늘만 회귀가 **3건** 났습니다 — 제가 만든 오류 1년 캐시, 연어 26건 무음, 그 AI의 KIG-005
무효 수정. 전부 사람이 나중에 발견했습니다. **테스트가 있었으면 즉시 걸렸을 것들입니다.**

상품화 후에는 이런 회귀가 **고객에게 먼저 발견**됩니다.

### 3-2. CI가 없습니다

```
.github/workflows : ❌ 없음
```

`main`에 push하면 Vercel이 **검사 없이 바로 배포**합니다. `tsc --noEmit`도 `build`도 사람이
기억해서 돌려야 합니다. 오늘 여러 번 제가 수동으로 돌렸습니다.

### 3-3. 장애를 알려줄 것이 없습니다

```
오류 수집(Sentry 등) : ❌ 없음
장애 알림            : ❌ 없음
헬스체크             : ✅ /api/media-health (제가 오늘 만든 것)
```

**새벽 3시에 사이트가 죽으면 아무도 모릅니다.** 고객이 알려줄 때까지요.

### 3-4. 백업 절차가 없습니다

> **→ 같은 날 해결됨 (커밋 `4c4d5a4`).** 이 감사 직후 `scripts/backup-license-data.mjs` 와
> `백업하기.bat` 이 추가되었습니다. R2 **읽기 전용**(List+Get)으로 날짜별 폴더에 객체와
> `manifest.json` 을 쓰고, 쓴 것을 즉시 검증한 뒤 최근 30개만 남깁니다.
> 저장 위치는 **저장소 밖**(`<repo>/../kig-backups`, `D:\kig-backup`, `%OneDrive%\KIG-백업`)입니다.
> 아래 내용은 감사 시점의 상태 기록으로 남겨 둡니다.

```
이용권·진도 저장 : R2 k-ig-license-private 버킷
백업 로직        : ❌ 없음
```

고객의 **이용권 등록 기록과 학습 진도**가 이 버킷 하나에 있습니다. 실수로 지우거나 손상되면
**복구 수단이 없습니다.** 유료 고객의 데이터입니다.

### 3-5. 응답 속도

```
/              1,523ms
/ld              821ms
/ld/d001          37ms   (캐시 적중)
/reading/pr001   506ms
```

홈이 1.5초입니다. 치명적이진 않지만 모바일에서는 체감됩니다. 측정만 해두고 넘어갑니다.

### 3-6. 다국어

`LanguageProvider`가 있는데 언어 토큰이 0건으로 잡힙니다. 껍데기만 있고 실제 번역은 없는
것으로 보입니다. **기능이 미완이면 노출하지 않는 편이 낫습니다.**

---

## 4. 결제·법적 (재확인 — 여전히 없음)

```
/terms  /privacy  /refund  /contact   전부 404
결제 연동                              ❌ 없음 (toss/portone/stripe 등 미설치)
```

**지금 구조로는 손님이 돈을 내도 관리자가 직접 이용권 키를 만들어 전달해야 합니다.**

---

## 5. 우선순위

### 출시 차단 (없으면 팔 수 없음)
1. **법적 문서 6종** — 전자상거래법·정보통신망법 요구사항
2. **결제 수단** — 또는 "계좌이체 + 수동 발급"을 공식 절차로 정하고 문서화

### 출시 전 강력 권장
3. **백업 절차** — 고객 이용권·진도. 복구 불가 위험
4. **장애 알림** — 죽은 걸 모르면 대응도 못 함
5. RE-016 오류 화면 · RE-014 제목 · RE-006 CSP

### 출시 후 가능
6. 자동 테스트 + CI
7. RE-008 사이트맵 · RE-004 · RE-005
8. 응답 속도 · 다국어 정리

---

## 6. 내일 (아카이브)

준비 완료:
- `scripts/archive-probe.cjs` — 8가지 질문에 **10초 내 답**. 가짜 아카이브로 동작 검증 완료
- `scripts/compare-archive.cjs` — 622건 대조 + `--full` 전수. 동작 검증 완료
- `evidence/archive-checklist.json` — 대조 항목 622건
- `ARCHIVE-PLAN.md` — 절차와 금지사항

**아카이브가 없어도 대부분 풀립니다** (이것도 오늘 확인했습니다):

| 대상 | 대체 경로 | 확인된 근거 |
|---|---|---|
| VOCA 뜻 3,877 | READING 어휘와 교차검증 + 사전 대조 | 877개 중복, 불일치 10건에 `live→라이브` 포함 |
| LISTENING 2,518문장 | 원본 음성 STT | 276/276 레슨에 음성 참조 존재 |
| READING 정렬·어휘 | 레슨 안 지문·번역에서 재생성 | 지문 238 · 번역 229 보유 |

**아카이브가 꼭 필요한 것은 4건뿐입니다**: GRAMMAR I 결번 6개, `gh1-033` 문항 37·39,
`gh1-040/088` 정답, `pr231` 번역.
