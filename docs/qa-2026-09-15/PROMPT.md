# 다른 AI에게 그대로 붙여넣을 프롬프트

> 최종 갱신: **2026-09-16 06:00** (8차 세션)
>
> ## 🔴 작업 지시는 이 문서가 아니라 `NEXT-SESSION.md` §0-Z 입니다
>
> 이 문서는 **변하지 않는 규칙**(작업 방식·금지사항·환경 함정)만 담습니다.
> **무엇을 할지**는 `NEXT-SESSION.md` §0-Z "지금 작업 대기열"에 있고, 그쪽이 항상 최신입니다.
> 아래 "당신이 할 일"과 "지금 상태 요약"은 2026-09-15 시점의 기록이며 **이미 낡았습니다** —
> 거기 적힌 KIG-005·006·014·022 등은 그 뒤에 처리되었습니다. 참고만 하고 따르지 마세요.

---

당신은 이 레포(K-IG CORE, Next.js 16 App Router · React 19 · TypeScript · Tailwind 4 · Vercel 배포, 운영 https://k-ig-core.vercel.app)의 출시 전 결함을 수정하는 개발자입니다.

**먼저 이 두 문서를 순서대로 전부 읽으세요.**

1. `docs/qa-2026-09-15/PROGRESS.md` — 지금까지 무엇이 끝났고 무엇이 남았는지. **가장 최신이며 이게 기준입니다.**
2. `docs/qa-2026-09-15/README.md` — 2026-09-15 감사에서 확인된 41건의 결함 원본 명세. 각 항목의 심각도·정확한 `파일:줄번호`·증거 파일·수정 지시·완료 조건이 있습니다.

증거 원본 데이터는 `docs/qa-2026-09-15/evidence/`, 재검증 스크립트는 `docs/qa-2026-09-15/scripts/`(감사용)와 `docs/qa-2026-09-15/scripts/verify/`(2차 세션 작성분)에 있습니다.

## 당신이 할 일

👉 **`docs/qa-2026-09-15/NEXT-SESSION.md` §0-Z "지금 작업 대기열"을 그대로 따르세요.**

<details>
<summary>아래는 2026-09-15 시점의 지시입니다 — 낡았으니 따르지 마세요 (기록 보존용)</summary>

**남은 19건 중 그룹 B·C·D(16건)** 입니다. PROGRESS.md §3 "다음 착수 순서"를 따르세요.

- **그룹 B (4건, 여기서 시작)** — KIG-005 채점 로직 → KIG-006 대안 정답 스키마 → KIG-014 진도 API 검증 → KIG-022 검색 인덱스
- **그룹 C (9건)** — KIG-010, 012, 017, 020, 021, 023, 025, 026, 027
- **그룹 D (3건)** — KIG-016, KIG-028/030, KIG-029

</details>

## 절대 건드리면 안 되는 것

- **PROGRESS.md §3 "⛔ 이미 완료된 이슈"의 22건.** 검증까지 끝났습니다. 다시 손대지 마세요.
- **그룹 E 4건 (KIG-003, 004, 007, 008).** 교재 원본이 있어야 하고, 소유자가 직접 진행합니다.
- **CNN.** 폐지 예정 페이지입니다. 고치지도, QA하지도, 음성 클립을 만들지도 마세요.
- **GVA.** 이미 폐지되었습니다(커밋 `276630e`). 복원하지 마세요.

## 작업 규칙

1. 이슈 **하나씩** 처리합니다. 수정 → PROGRESS/README에 적힌 완료 조건 실행 → 결과 보고 → 커밋(`fix(KIG-005): ...`). 다음 이슈로 넘어가기 전에 확인받으세요.
2. **검증하지 않은 것을 완료라고 하지 마세요.** 실행한 명령과 출력을 그대로 보여주세요. 가장 확실한 방법은 **수정을 되돌렸을 때 같은 프로브가 실패하는지** 확인하는 것입니다. 2차 세션은 전 항목을 그렇게 검증했습니다(PROGRESS.md §4 참조).
3. **데이터를 지워서 "해결"하지 마세요.** 원본이 기준입니다. 원본을 구할 수 없으면 그 사실을 알리고 해당 기능 비노출을 제안하세요.
4. **🔴 문장·단어 텍스트를 바꾸면 음성 클립을 반드시 다시 구우세요.** 클립 키가 텍스트 해시라서, 텍스트가 바뀌면 그 문장은 소리가 나지 않습니다. KIG-002가 이 방식으로 READING 음성을 40.6%까지 떨어뜨렸습니다. 절차는 PROGRESS.md §3 끝에 있습니다. 그룹 C·D는 대부분 여기 해당합니다.
   - **음성 작업 전에 `.env.local`이 있어야 합니다.** 이 파일은 비밀값이라 GitHub에 없습니다. 소유자에게 요청하세요. `public/audio`도 gitignore라 클론에는 클립이 2개뿐인데, 자격증명이 있으면 스크립트가 R2를 조회해 실제 보유분(38,398개)을 인식합니다. 자격증명 없이 생성을 돌리면 전 코퍼스를 다시 구우려 하고 무료 한도(월 50만 자)를 2.5배 초과합니다 — 스크립트가 경고하지만, 경고를 무시하지 마세요.
5. **UI 문구를 새로 쓸 때 음성의 출처를 언급하지 마세요.** "원어민 음성"도, "AI 음성"도 쓰지 않습니다. 기능만 설명하세요("음성 듣기", "발음 듣기"). 소유자 결정입니다(PROGRESS.md §1-B).
6. **운영 데이터에 쓰지 마세요.** Cloudflare R2 버킷과 Vercel 환경변수는 건드리지 않습니다. 로컬 유료 레슨 검증은 PROGRESS.md §5의 절차를 쓰세요(작업 후 fixture 파일 삭제 필수).
7. **`.env.local`과 `.vercel/`의 값은 절대 출력하거나 커밋하지 마세요.** 스크린샷도 안 됩니다.
8. `main`에 push하면 Vercel이 자동 배포됩니다. 배포 후 운영 URL에서 재확인하세요.
9. PROGRESS.md §4 "환경 함정"을 먼저 읽으세요. Tailwind 캐시, dev origin 제한 등 시간을 크게 잡아먹는 함정이 정리되어 있습니다.

10. **🔴 세션이 끝나기 전에 반드시 인계 기록을 남기세요.** 이 작업은 여러 AI가 이어받습니다. 당신의 대화 기록은 다음 사람에게 전달되지 않고, **레포에 커밋된 것만 남습니다.**
    - 사용량 한도가 가까워지거나, 작업을 멈추거나, 사용자가 "정리해"라고 하면 **그 즉시** 아래를 하세요.
    - `docs/qa-2026-09-15/PROGRESS.md`에 추가할 것: **완료한 이슈**(커밋 해시·검증 결과·측정 수치), **작업 중이던 이슈와 어디까지 했는지**, **새로 발견한 결함**, **막힌 것과 그 이유**, **다음 사람이 바로 이어받을 지점**.
    - 반쯤 고친 코드를 그대로 두지 마세요. 완성해서 커밋하거나 `git restore`로 되돌리거나 둘 중 하나입니다. 어중간한 상태를 넘기면 다음 사람이 그게 의도인지 실수인지 알 수 없습니다.
    - 마지막에 **커밋하고 `git push origin main`** 하세요. 푸시하지 않으면 없던 일이 됩니다.
    - 검증에 쓴 스크립트는 `docs/qa-2026-09-15/scripts/verify/`에 커밋하세요. 다음 사람이 같은 것을 다시 만들지 않아도 되고, 회귀 확인에도 씁니다.

## 🔴 동시 작업 — 이 저장소는 여러 AI가 함께 씁니다

- **`git add -A` / `git add .` 금지.** 파일을 **이름으로 지정해서만** add 하세요.
  다른 세션의 미커밋 작업이 항상 워킹트리에 있습니다. 어느 파일이 남의 것인지는
  `NEXT-SESSION.md` §0-Z 맨 아래 목록에 있습니다.
- `git stash` 금지 (이 저장소에서 `fatal: unable to read tree` 이력).
- 푸시 확인은 `git ls-remote origin refs/heads/main` 으로. `git log origin/main` 은 낡은 값을 보여줍니다.

## 검증에 대해 — 실제로 있었던 실패

**상태 코드가 아니라 화면에 나온 글자로 판정하세요.** 2026-09-16 에 404 화면 수정이
"5개 경로 전부 통과"로 보고되었지만, 프로브가 상태 코드와 "응답 어딘가에 문구가 있는가"만
봤습니다. 문구는 `<script>` 안 RSC 페이로드에 있었고 **서버 렌더 HTML 은 비어 있었습니다.**
손님은 백지를 봤습니다. 경로 수는 늘렸지만 **검사 축**이 하나뿐이었습니다.

판정은 이렇게 하세요: `<body>` → `<script>` 제거 → 태그 제거 → 남은 글자 수.

**타입 검사는 `.cjs`/`.mjs` 호출부를 잡지 못합니다.** 같은 날 `getCollocation` 의 인자를
바꾼 커밋이 데이터 감사 도구를 죽이고(예외) 음성 수집기를 **에러 없이 0개**로 만들었습니다.
TS 함수 시그니처를 바꾸면 반드시 `.cjs`/`.mjs` 호출부를 grep 하세요.

## 시작 방법

1. `PROGRESS.md` 와 `NEXT-SESSION.md` 를 읽고 **읽은 내용을 요약**해서 보여주세요.
2. `NEXT-SESSION.md` §0-Z 의 **맨 위 항목**에 대한 수정 계획을 **코드 인용과 함께** 제시하세요.
3. 소유자 승인을 받은 뒤에 진행하세요. **승인 없이 커밋하지 마세요.**

---
