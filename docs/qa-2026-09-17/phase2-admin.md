# Phase 2 — 관리자 경로 (읽기 전용, 운영 `/admin/license`, 2026-09-17)

접근: 앱 내 브라우저 탭 `seed` (소유자가 관리자 PIN 입력). **누른 버튼 없음** — 화면 구조는 DOM 읽기(`javascript_tool`, 이용권 코드·기기 ID 는 읽는 즉시 가림), 동작은 소스 `src/app/admin/license/page.tsx` 로 확인.
관리자 기능 범위(Phase 1): 이용권 발급·기기 현황·기기 한도·기기 초기화·환불 차단/해제·STUDENT 진도 보기/수동 해금/초기화. **과정·레슨·가격·회원 관리 기능은 없음** (콘텐츠는 저장소 파일로만 바뀜 → "관리자 경로로 유료 과정 점검"은 해당 없음, Phase 5 에서 파일·운영 렌더링으로 전부 점검).

| ID | 심각도 | 문제 | 근거 | 영향 | 권장 | 확인 |
|---|---|---|---|---|---|---|
| **ADM-01** | **High (운영)** | **관리자 목록이 서버가 아니라 "발급한 그 브라우저"의 기록만 보여 줌. 코드 검색 칸 없음** | 목록은 `history`(= 관리자 브라우저 localStorage `kig:admin:history`, 최대 100개)만 순회 (`page.tsx:715-721`); 서버 기록(`deviceRecords`)은 목록에 있는 코드의 상태를 채우는 데만 씀 | 다른 PC·다른 브라우저·브라우저 데이터 삭제 후·101번째 이후 코드는 **환불 차단·기기 초기화·진도 조회를 화면에서 할 수 없음**. 환불 고객 차단이 불가능해질 수 있음 | 서버 기록 전체 목록 + 코드 검색 (메모도 서버에 저장) | **운영 확인**: 이 관리자 창은 LIFE 코드가 실제 등록·사용 중인데 "아직 발급된 내역이 없습니다" 표시 (`find` 결과 ref_67) |
| ADM-02 | Medium — ✅ 수정 (버튼 삭제, 배포 `25f8c1d`) | "🧪 내 기기에 등록 테스트" 가 **고객 코드**에 관리자 브라우저를 기기로 등록 (확인 창 없음) | `page.tsx:809-818` 는 기기 0대인 모든 코드(= 막 발급한 모든 고객 코드)에 표시, `handleTestRegister` → `/api/license/activate` (`:222-244`) | 한 번 누르면 고객의 기기 칸 하나를 관리자 PC 가 차지 (2대 한도면 고객은 1대만 남음) | 테스트 전용 코드에서만 표시하거나 확인 창 + 등록 직후 자동 해제 | 코드 확인 (누르지 않음) |
| ADM-03 | Medium — ✅ 수정 (확인 창, 배포 `25f8c1d`) | 기기 한도 드롭다운이 **고르는 즉시** 서버에 저장 (확인 없음) | `page.tsx:775-787` `onChange → handleUpdateMaxDevices` | 키보드 화살표·실수 선택만으로 고객 한도가 줄어듦 | 변경 후 [저장] 버튼 또는 확인 창 | 코드 확인 (조작하지 않음) |
| ADM-04 | Medium — ✅ 수정 (확인 창, 배포 `25f8c1d`) | STUDENT "수동 해금" 드롭다운도 **고르는 즉시** 고객 진도 챕터를 바꿈 | `page.tsx:890-898` `onChange → requestStudentProgress(…, "setChapter")` | 실수로 낮추면 학습자가 이미 연 챕터가 다시 잠김 | 확인 창 | 코드 확인 |
| ADM-05 | Low — ✅ 수정 (81 표기 제거, 배포 `25f8c1d`) | STUDENT 강의 수 표기 `81강`, `완료 강의 x/81` | `page.tsx:498,548-549,883`; STUDENT 과정 목록은 **82강** (`content/courses/student.json` 20챕터 82개, 파일 87개 = 개요 페이지 5 포함) | 관리자·판매 문구와 실제 강의 수 불일치, 완료 수가 81 을 넘을 수 있음 | 과정 데이터에서 수를 읽어 표시 | 데이터 대조 |
| ADM-06 | Low — ✅ 수정 (서버 기록에 메모, 배포 `25f8c1d`) | 발급 메모(예시 문구 "스마트스토어 홍길동님 주문")가 관리자 브라우저 localStorage 에만 저장 | `page.tsx:164-183`, 서버 기록 형식에 메모 없음 (`deviceStorage.ts:19-27`) | 고객 이름이 관리자 PC 브라우저에 평문으로 남고, 다른 PC 에선 안 보임 | 서버 암호화 기록에 메모 저장 | 코드 확인 |
| ADM-07 | Low (확장성) — ✅ 수정 (바뀐 기록만 내려받기·동시 16개, 배포 `de4cbcf`). 가짜 R2 1,000건 시험 `verify-device-records-cache.cjs` 6/6: 첫 새로고침 1,000건 중 동시 ≤16, 변화 없으면 0건, 3건 바뀌면 3건 (수정 전 코드: 동시 1,000, 매번 1,000건) | "기기 현황 새로고침" 이 R2 의 모든 이용권 기록을 목록 조회 후 **하나씩** 내려받아 복호화 | `deviceStorage.ts:199-236` | 고객 수에 비례해 느려지고 R2 요청 비용 증가 (고객 1,000명 = 새로고침 1번에 GET 1,000회) | 페이지 단위 조회·색인 | 코드 확인 |

확인 창이 **있는** 동작 (코드 확인): 기기 초기화 (`:271`), 환불 차단 (사유 입력 `:294`), 차단 해제 (`:316`), 진도 초기화 (확인 2번 `:362-363`), 목록 지우기 (`:262`).
관리자 인증 자체의 위험(단일 PIN, 2차 인증 없음 등)은 `phase8-security.md` §3 — **보고만, DEFERRED — OWNER/LAWYER**.
