# Phase 4 — 기능·기술 (운영, 평생(LIFE) 이용권, 2026-09-17)

작업자 = 점검자 = Claude.

## 4-1. 전 레슨 × 데스크톱·모바일 × 모든 단계 — `scripts/sweep-licensed.cjs --suffix -v2` → `out/sweep-licensed-v2.jsonl`, 집계 `scripts/analyze-sweep.cjs` → `out/sweep-summary.json`

**v1 폐기 사유 (스크립트 결함, 사이트 결함 아님):** 이동 직후 이전 문서의 `readyState` 로 로딩 완료를 판단 · 페이지마다 sessionStorage 를 지워 이용권 쿠키 새로고침을 매번 유발 · React `:r1:` id 를 CSS 선택자에 그대로 넣어 입력칸 있는 화면에서 검사 실패 → 3,496건 중 1,110건 스냅샷 없음, 최소 31건 다른 과정 화면. v1 파일은 증거로 보존.
**v2 방식:** 빈 탭 → 목표 URL 도달 + 해당 과정 `"… 목록"` 링크 렌더링 확인 → 2초 동안 새로고침이 없을 때까지 대기 → 스냅샷 → 단계 버튼마다 클릭·스냅샷. 창 4개. 중간에 세션이 끊겨(3,062건) 같은 명령으로 이어서 완료.

| 과정 | 방문 (2폭) | 정상 로딩 | 설계상 이동 | 문제 없음 | 단계 클릭 |
|---|---|---|---|---|---|
| LISTENING | 1,104 | 1,104 | 0 | 1,104 | 5,520 |
| READING | 1,024 | 1,024 | 0 | 1,024 | 4,096 |
| STUDENT | 174 | 174 | 0 | 174 | 522 |
| VOCA | 390 | 390 | 0 | 390 | 1,560 |
| GRAMMAR I | 388 | 194 | **194** | 194 | 776 |
| GRAMMAR II | 176 | 176 | 0 | 176 | 704 |
| CNN (폐지) | 240 | 240 | 0 | 238 (+2 오탐) | 720 |
| **합계** | **3,496** | **3,302** | **194** | **3,302** | **13,898** |

- **설계상 이동 194건:** GRAMMAR I 홀수(답안) 쪽 주소 97개 × 2폭은 짝수(문제) 통합 페이지로 서버가 보냄 (`src/app/[course]/[lesson]/page.tsx:131-139`). 짝 페이지 방문으로 화면을 봄.
- **기록된 문제 0건** (단계 행 전체): 콘솔 오류, 잡히지 않은 예외, CSP·로그 오류, 4xx·5xx 응답, 실패한 요청, 이용권 있는데 잠금 화면, 가로 넘침, 라벨 없는 입력칸, 150자 미만 렌더링, `undefined`/`NaN`/`[object Object]` 노출, 오류 화면, 404 제목, 깨진 이미지. 걸린 2건은 CNN cnn060 본문 문장 "…프로그램을 **준비 중**인데…" (자리표시 아님, 오탐).
- 새로고침 4건 = 워커 탭마다 첫 유료 레슨 1회 (SEC-08).
- 24px 미만 조작 요소: 거의 모든 단계에 있음 → 무엇인지 Phase 6 UX-05·06.

## 4-2. 화면에 실제로 보이는 글 — `scripts/scan-rendered.cjs` → `out/rendered-scan.json` (데스크톱 1,748개 레슨 전 단계 텍스트)

| 패턴 | 레슨 | 판정 |
|---|---|---|
| 어휘 주석 `*nearsighted: 근시안의`, `*torso: 몸통` 등 | 6 (pr048·pr115·pr237 본문·스크립트) | **결함 화면 노출 확인** (R-14·R-34·R-48) |
| 시험 보기 `① … ③ ③ …` | 2 (pr056 본문·스크립트) | **확인** (R-21) |
| 연도 `7904년` | 2 (pr193) | **확인** (R-51) |
| 밑줄 기호 `(a)the big ball` | pr237 | **확인** (R-48). 나머지 4건은 `judg(e)ment` `medi(a)eval` `bongo(e)s` 표기법 (정상) |
| `(핵심 어휘)` 자리표시 | 0 | 카드 뜻은 "뜻 보기" 전에는 화면에 없어서 이 방법으로는 안 잡힘 — 데이터로 69장 확인 (RV) |
| `PASS-OFF`, 옛 제목 `:::`, `undefined`, 깨진 문자 | 0 | |

## 4-3. 레슨 음성 — `scripts/audio-check.cjs` → `out/audio-check.json`, `scripts/probe-missing-audio.cjs` → `out/missing-audio.json` `out/player-probe.json`

| 확인 | 결과 |
|---|---|
| 레슨 데이터가 가리키는 음성 파일 3,233건(서로 다른 2,600개) — LIFE 쿠키로 2바이트 요청 | **3,228 정상 (206, audio)**, 5건 실패: STUDENT s10-4·s10-5 → `/audio/middle/p104·p105.mp3` **403** (다른 과정 폴더), GRAMMAR I gh1-020·021 → `gh1-020.mp3`·`gh1-021.mp3` **404** (파일 없음) |
| 같은 파일을 이용권 없이 | 유료 3,187건 **모두 403**, 무료 46건 206 — 유출 0 |
| 음성 데이터가 없는 레슨 | CNN 120 (영상 과정), STUDENT 개요 s1–s5 |
| **실패 5건의 화면 영향** | 없음 — 세 레슨 모두 재생을 누르면 문장별 Azure 음성(206) 재생, 실패한 mp3 는 요청조차 안 함 |
| **플레이어가 실제로 트는 소리** | CNN 외 모든 과정이 원본 녹음이 아니라 **대본의 Azure 합성 음성** (`src/lib/unifiedSpeech.ts:42-47`; `/ld/d167` `/reading/pr100` `/grammar2/gh2-030` `/phonics/hv-48` 재생 시 `audio/azure-ava/v1/…` 만 요청) |

| ID | 심각도 | 문제 | 근거 | 권장 |
|---|---|---|---|---|
| F-01 | Low — ✅ 수정 (배포 `de4cbcf`): 죽은 주소 5개(4레슨) 데이터에서 제거, 파일은 그대로 (`apply-f01.cjs`). `verify-f01.cjs` (저장소 목록 읽기 전용): 운영 경로 레슨의 음성 주소 **3,228개 전부 존재·자기 과정 폴더** (수정 전 5건 실패). s10-4·s10-5 에만 떠 있던 여분의 "전체 듣기" 막대도 없어짐 | 레슨 데이터의 죽은 음성 주소 5건 (다른 과정 폴더 403, 없는 파일 404) — 화면 영향 없음 | 위 표 | 데이터에서 제거 또는 올바른 파일로 |
| F-02 | Low (운영비) | 원본 녹음 유료 파일 3,187개가 저장·잠금 처리돼 있지만 CNN 외에는 재생되지 않음 | 위 표 | **소유자 결정 (2026-09-17): 계속 보관.** 삭제·정리하지 말 것 |
| MEDIA-01 | Medium — ✅ 수정 (로컬 검증, 배포 대기, 2026-09-18). 저장소(R2)에서 받는 4초 제한을 "응답 헤더까지" 로만 적용, 본문은 끝까지 흐르게 함. **1차 수정이 새 결함을 만들었고 배포 전 반박형 리뷰가 잡음:** 옛 4초 제한이 읽지 않은 본문(HEAD 요청·저장소 상태 점검)의 연결을 풀어 주던 유일한 장치라, 제한만 없애면 연결 50개가 쌓인 뒤 그 서버의 음원이 전부 502. → 본문을 이 모듈이 통제하는 스트림(`toWebStream`)으로 넘김: 취소하면 연결을 닫고, 읽는 중 20초간 아무것도 안 오면 실패 처리. HEAD·상태 점검·404/416/502 응답은 본문을 취소. **2차 리뷰가 또 하나 잡음:** 저장소 응답 전에 사용자가 떠나면(다음 문장으로 넘김) Next 가 응답을 아예 보내지 않아 본문이 방치됨 — 로컬 `next start`+R2 로 재현(60번 뒤 다음 음원 4.5초 후 502). → 요청의 중단 신호로 본문 취소, 그리고 누가 놓친 경로라도 **아무도 읽기 시작하지 않은 본문은 120초 뒤 연결 반환** 안전장치. (3차 리뷰: 처음엔 읽는 중인 본문에도 걸려 있어, 미리 조금 받고 재생을 기다리는 플레이어가 120초 뒤 끊김 → 첫 조각을 읽으면 해제되게 좁힘. 대조: 해제 줄을 빼면 멈춘 사용자가 2,407바이트에서 끊김.) **검증 `verify-media-origin-stream.cjs` 18/18** (실제 R2 읽기 전용): 5초 멈춤 읽기 — 커밋된 코드 1,369/3,077,433 바이트에서 끊김, 수정본 전부; 읽지 않은 본문 60개 뒤 — 1차 수정 401·4.4초(누수 재현), 수정본 206·73 ms; 아무도 취소 안 한 본문 60개 — 안전장치(시험용 1.5초) 뒤 206·87 ms (대조: 실제 120초 설정에선 2.5초 뒤에도 401·막힘); 중간에 끊은 다운로드 20개 뒤 206; 멈춘 스트림 300 ms 뒤 실패·정지된 읽기는 안 끊김; 로컬 `next start` 에서 HEAD 60번·클라이언트 중단 20번·**응답 전 끊김 60번** 뒤 각각 206·약 0.1초, 3 MB 전체 다운로드·상태 점검 정상. 반박형 리뷰 3회. 작성·확인 같은 에이전트 | 큰 음성·영상이 4초 넘게 걸리면 이미 200/206 과 전체 길이를 보낸 뒤 **중간에 잘림** (`mediaOrigin.ts` `AbortSignal.timeout(4000)` 이 본문 전송 내내 유지) — 살아 있는 과정 최대 2.9 MB 는 iad1 에서 1.9~2.2초로 여유 약 2배, 폐지된 CNN 영상(최대 22.8 MB)은 초과 | 2026-09-17 조사·재현 (위 스크립트 1번) | 헤더 도착 뒤 제한 해제 + 연결 해제 보장 |
| MEDIA-02 | Medium — ✅ 수정 (로컬 확인, 운영 확인은 배포 후, 2026-09-18). 무료 음원도 `private, max-age=31536000, immutable` — 브라우저에만 1년 저장, CDN 은 저장 안 함 → 모든 부분 요청이 우리 서버로 와서 정상 206. 로컬엔 Vercel CDN 이 없어 **수정 효과는 배포 후 운영 `probe-media-range-cache.cjs` 로만 증명 가능** | **무료 음원이 Vercel CDN 에 한 번 저장되면, 부분 요청(Range)에 206 이 아니라 200 + 요청한 조각만** 돌려줌 — 플레이어는 그 조각을 파일 전체로 오해. Safari 는 모든 음원을 `bytes=0-1` 로 먼저 요청하므로 저장된 무료 음원이 2바이트짜리 파일로 보일 수 있음 | 운영 측정 2026-09-18 `probe-media-range-cache.cjs`: 전체 GET 뒤 `bytes=0-1` → 200·2바이트(HIT), `bytes=100-199` → 200·100바이트 | CDN 이 음원을 저장하지 않게 (`private`) |

## 4-4. 시험하지 않은 기능 (이유)

- 마이크 발음 평가(VOCA STEP 3, LISTENING 섀도잉, READING 낭독): 실제 음성 입력 필요 — 헤드리스 브라우저로 불가 → **BLOCKED (실기기 필요)**.
- STUDENT 완료 체크·챕터 해금 저장, 북마크·완료 체크: 감사용 이용권의 운영 진도가 바뀜 → **Read-only verification required** (서버 API 인증·요청 제한은 코드·401 탐침으로 확인).
- UI 언어 en/ja/zh 전환: 이번 스윕은 한국어 UI 만 → **미시험**.
- 카카오톡 인앱 브라우저 안내, 실제 iPhone Safari·Android Chrome: **미시험 (실기기)**.

## 4-5. 배포 886b08c 후 READING 재스윕 (2026-09-17, `-v3`)

ISS-00 수정이 클라이언트의 전체 지문 예비 조회를 없앴으므로, 모든 READING 페이지가 자기 데이터만으로 제대로 나오는지 운영에서 다시 확인. LIFE 이용권 감사 프로필, 데스크톱·모바일.

| 항목 | 결과 | 근거 |
|---|---|---|
| 방문 | 1,024 / 1,024 열림 (512페이지 × 2), 문제 기록 **0**, 단계 클릭 4,096 | `sweep-licensed.cjs --course reading --suffix -v3` → `analyze-sweep.cjs --suffix -v3` |
| 음성 | 재생 512회 눌림, 요청 오류 0 (요청 195건 — 나머지는 캐시) | 같은 결과 |
| 지문이 실제로 보이는가 | **512 / 512 페이지** — 본문 영어·대본 한국어 **2,892 / 2,892문장**, 카드 14개 순서대로 **256 / 256** | `check-reading-rendered-all.cjs --suffix -v3` |
| 교체 지문 pr151·pr170 | 4/4 — 새 문장·카드, 옛 표현 0, 음성 36/36 | `check-r64-r65-rendered.cjs`, `check-r64-r65-audio.cjs` |

작업자와 점검자가 같음 (Claude). 실제 iPhone·Android 는 확인 안 함.